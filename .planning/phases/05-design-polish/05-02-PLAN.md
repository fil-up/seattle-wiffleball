---
phase: 05-design-polish
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/app/page.tsx
  - src/app/stats/players/page.tsx
  - src/app/stats/players/[id]/page.tsx
  - src/app/stats/teams/page.tsx
  - src/app/stats/teams/[team]/page.tsx
  - src/app/teams/page.tsx
  - src/app/teams/[id]/page.tsx
  - src/app/leaderboards/page.tsx
  - src/components/PageNavigation.tsx
  - src/components/Navigation.tsx
  - src/components/StandingsWidget.tsx
  - src/components/LeaderboardPodium.tsx
  - src/components/PlayerBattingData.tsx
  - src/components/PlayerPitchingData.tsx
autonomous: true

must_haves:
  truths:
    - "No page renders its own navigation bar — all navigation comes from layout.tsx"
    - "Core pages use theme tokens and display correctly in both light and dark mode"
    - "Data-fetching core pages show skeleton placeholders while loading"
    - "Data-fetching core pages show a friendly error message with retry button on failure"
    - "Stale data banners are styled consistently with the brand theme"
  artifacts:
    - path: "src/components/PageNavigation.tsx"
      provides: "DELETED — no longer exists"
    - path: "src/components/Navigation.tsx"
      provides: "DELETED — no longer exists"
    - path: "src/app/page.tsx"
      provides: "Home page without inline nav, using theme tokens"
    - path: "src/app/stats/players/page.tsx"
      provides: "Players stats page with theme tokens, skeleton, and error state"
  key_links:
    - from: "core page files"
      to: "src/components/ErrorState.tsx"
      via: "import and render on fetch failure"
      pattern: "import.*ErrorState"
    - from: "core page files"
      to: "src/components/Skeleton.tsx"
      via: "import and render while loading"
      pattern: "import.*Skeleton"
---

<objective>
Migrate all core pages (home, stats, teams, leaderboards) to the new theme system: remove per-page navigation, apply theme tokens for dark/light mode support, and add skeleton loading states and error handling.

Purpose: Core pages are the most-visited pages on the site. This migration ensures they look consistent with the new design system and support dark mode.
Output: All 8 core pages themed and polished, old navigation components deleted, loading and error states added.
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-design-polish/05-01-SUMMARY.md
@src/app/page.tsx
@src/app/stats/players/page.tsx
@src/app/stats/players/[id]/page.tsx
@src/app/stats/teams/page.tsx
@src/app/stats/teams/[team]/page.tsx
@src/app/teams/page.tsx
@src/app/teams/[id]/page.tsx
@src/app/leaderboards/page.tsx
@src/components/StandingsWidget.tsx
@src/components/LeaderboardPodium.tsx
@src/components/PlayerBattingData.tsx
@src/components/PlayerPitchingData.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old navigation and apply theme tokens to core pages</name>
  <files>src/app/page.tsx, src/app/stats/players/page.tsx, src/app/stats/players/[id]/page.tsx, src/app/stats/teams/page.tsx, src/app/stats/teams/[team]/page.tsx, src/app/teams/page.tsx, src/app/teams/[id]/page.tsx, src/app/leaderboards/page.tsx, src/components/PageNavigation.tsx, src/components/Navigation.tsx, src/components/StandingsWidget.tsx, src/components/LeaderboardPodium.tsx, src/components/PlayerBattingData.tsx, src/components/PlayerPitchingData.tsx</files>
  <action>
    **Step 1: Delete old navigation files**
    - Delete src/components/PageNavigation.tsx
    - Delete src/components/Navigation.tsx (stale/unused)

    **Step 2: Remove navigation from all 8 core pages**
    For each of these pages, remove the `import PageNavigation` line and every `<PageNavigation />` JSX usage:
    - src/app/stats/players/page.tsx
    - src/app/stats/players/[id]/page.tsx
    - src/app/stats/teams/page.tsx
    - src/app/stats/teams/[team]/page.tsx
    - src/app/teams/page.tsx
    - src/app/teams/[id]/page.tsx
    - src/app/leaderboards/page.tsx

    **Step 3: Remove inline navigation from home page**
    In src/app/page.tsx, remove the entire sticky nav section (the `<div className="sticky top-0 z-50">` block containing all the inline nav links and the gold accent line, approximately lines 52-103). The SiteNavigation in layout.tsx replaces it.

    **Step 4: Apply theme color tokens across all core pages and shared components**
    Systematic replacement in all 8 page files AND shared components (StandingsWidget, LeaderboardPodium, PlayerBattingData, PlayerPitchingData):

    | Old Class | New Class |
    |-----------|-----------|
    | `bg-gray-50` | `bg-surface-secondary` |
    | `bg-white` | `bg-surface-card` |
    | `bg-gray-100` | `bg-surface-secondary` |
    | `text-gray-900` | `text-content-primary` |
    | `text-gray-800` | `text-content-primary` |
    | `text-gray-700` | `text-content-primary` |
    | `text-gray-600` | `text-content-secondary` |
    | `text-gray-500` | `text-content-secondary` |
    | `text-gray-400` | `text-content-secondary` |
    | `border-gray-200` | `border-border` |
    | `border-gray-300` | `border-border` |
    | `divide-gray-200` | `divide-border` |
    | `bg-[#25397B]` | `bg-brand-navy` |
    | `text-blue-600` | `text-brand-navy` |
    | `hover:text-blue-800` | `hover:text-brand-navy/80` |
    | `bg-blue-100` | `bg-brand-navy/10` |
    | `text-blue-800` | `text-brand-navy` |
    | `hover:bg-gray-50` | `hover:bg-table-hover` |
    | `hover:bg-gray-100` | `hover:bg-table-hover` |

    For the home page specifically:
    - `min-h-screen bg-gray-50` → `min-h-screen bg-surface-secondary`
    - Keep hero section's bg-[#25397B] gradient as-is (it's a visual effect, not a theme-able surface)
    - Quick links section `bg-gray-100` → `bg-surface-secondary`

    **Step 5: Restyle stale data banners**
    Find all stale data banners (bg-yellow-50 with yellow styling) and restyle to:
    `bg-brand-gold/10 border border-brand-gold/30 text-content-primary` with a small warning icon. This looks cohesive in both light and dark modes.
  </action>
  <verify>
    - `npx next build` compiles without errors (no broken imports from deleted PageNavigation)
    - No file in the project imports PageNavigation or Navigation (grep confirms)
    - Home page has no inline nav markup
    - Core pages use theme tokens instead of hardcoded gray/white colors
  </verify>
  <done>All core pages use the unified navigation from layout.tsx. No page has its own nav bar. All hardcoded color classes replaced with theme tokens. Stale banners restyled with brand colors.</done>
</task>

<task type="auto">
  <name>Task 2: Add skeleton loading and error states to core data-fetching pages</name>
  <files>src/app/page.tsx, src/app/stats/players/page.tsx, src/app/stats/players/[id]/page.tsx, src/app/stats/teams/page.tsx, src/app/stats/teams/[team]/page.tsx, src/app/teams/page.tsx, src/app/teams/[id]/page.tsx, src/app/leaderboards/page.tsx, src/components/StandingsWidget.tsx</files>
  <action>
    For each page that fetches data, replace "Loading..." text or bare conditional renders with proper skeleton and error components.

    **Import in each file:**
    ```tsx
    import { TableSkeleton, CardSkeleton } from '@/components/Skeleton'
    import ErrorState from '@/components/ErrorState'
    ```

    **Add error tracking state:**
    Each page that has a fetch should add:
    ```tsx
    const [error, setError] = useState(false)
    ```
    In the catch block of each fetch, add `setError(true)` alongside existing console.error.

    **Per-page replacements:**

    1. **Home page (src/app/page.tsx)**:
       - No loading state needed for news (articles render when ready, cards area is already visually complete)
       - Add error state is not needed here (already has graceful empty state)

    2. **Players stats (src/app/stats/players/page.tsx)**:
       - Replace the `if (loading) return <div>Loading...</div>` with `if (loading) return <div className="container mx-auto px-4 py-8"><TableSkeleton rows={12} cols={8} /></div>`
       - Add: `if (error) return <div className="container mx-auto px-4 py-8"><ErrorState message="Couldn't load player stats." onRetry={() => { setError(false); /* re-trigger fetch by toggling scope */ }} /></div>`
       - Show error state BEFORE loading check so errors take priority

    3. **Player detail (src/app/stats/players/[id]/page.tsx)**:
       - Replace loading state with `<TableSkeleton rows={6} cols={5} />`
       - Add error state with retry

    4. **Teams stats (src/app/stats/teams/page.tsx)**:
       - Replace loading with `<TableSkeleton rows={10} cols={6} />`
       - Add error state with retry

    5. **Team stats detail (src/app/stats/teams/[team]/page.tsx)**:
       - Replace loading with `<TableSkeleton rows={10} cols={8} />`
       - Add error state with retry

    6. **Teams index (src/app/teams/page.tsx)**:
       - Replace loading with grid of `<CardSkeleton />` (6 items in grid matching team card layout)
       - Add error state with retry

    7. **Team detail (src/app/teams/[id]/page.tsx)**:
       - Replace loading with combination: `<CardSkeleton />` for header + `<TableSkeleton />` for stats
       - Add error state with retry

    8. **Leaderboards (src/app/leaderboards/page.tsx)**:
       - Replace loading with `<CardSkeleton />` grid (matching podium layout)
       - Add error state with retry

    9. **StandingsWidget (src/components/StandingsWidget.tsx)**:
       - If it has a loading state, replace with compact `<TableSkeleton rows={6} cols={3} />`
       - This widget already has skeleton animation — verify and keep if adequate

    For the retry mechanism, the simplest pattern is to set error to false and re-trigger the fetch. Most pages use a useEffect triggered by state changes. Set a `retryCount` state and include it in the useEffect dependency array:
    ```tsx
    const [retryCount, setRetryCount] = useState(0)
    // In useEffect deps: [tab, scope, retryCount]
    // In ErrorState: onRetry={() => { setError(false); setRetryCount(c => c + 1) }}
    ```
  </action>
  <verify>
    - `npx next build` compiles without errors
    - Each data-fetching page imports TableSkeleton or CardSkeleton
    - Each data-fetching page has an error state with ErrorState component
    - No page shows bare "Loading..." text
  </verify>
  <done>All core data-fetching pages show skeleton loading placeholders instead of text. All show friendly error messages with retry buttons on fetch failure. Loading and error states work in both light and dark themes.</done>
</task>

</tasks>

<verification>
1. `npx next build` passes
2. `grep -r "PageNavigation" src/` returns zero results (fully removed)
3. `grep -r "Loading\.\.\." src/app/ src/components/` returns zero results in core pages
4. Visit each core page in dev mode — verify theme tokens render correctly in both light and dark mode
5. Simulate network error — verify ErrorState renders with retry button
</verification>

<success_criteria>
- Zero pages import or render PageNavigation
- Home page has no inline navigation
- All 8 core pages use theme tokens for colors
- All data-fetching core pages show skeletons while loading
- All data-fetching core pages show ErrorState on failure
- Stale data banners use brand-consistent styling
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-polish/05-02-SUMMARY.md`
</output>
