---
phase: 02-stats-engine
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/app/stats/players/page.tsx
  - src/components/StatsTable.tsx
autonomous: true

must_haves:
  truths:
    - "Default hitting sort is wRC+ descending (not OPS)"
    - "Default pitching sort is ERA ascending (lower is better)"
    - "First click on a counting stat (HR, RBI, K, etc.) sorts descending; first click on a rate stat (ERA, WHIP, OPP AVG) sorts ascending"
    - "Totals view shows a Seasons column between Player and the first stat column"
    - "Yearly view does not show a Year column"
    - "Both hitting and pitching tables render correctly with sortable columns in both yearly and totals scope"
  artifacts:
    - path: "src/app/stats/players/page.tsx"
      provides: "Column definitions with sortDescFirst metadata and correct sort defaults"
      contains: "sortDescFirst"
    - path: "src/components/StatsTable.tsx"
      provides: "Table component that respects per-column sortDescFirst"
      contains: "sortDescFirst"
  key_links:
    - from: "src/app/stats/players/page.tsx"
      to: "src/components/StatsTable.tsx"
      via: "columns with sortDescFirst passed to StatsTable"
      pattern: "sortDescFirst"
    - from: "src/components/StatsTable.tsx"
      to: "@tanstack/react-table"
      via: "useReactTable with column sortDescFirst metadata"
      pattern: "useReactTable"
---

<objective>
Add smart sort direction to stats tables so first-click sorts in each stat's natural direction (descending for counting stats like HR, ascending for rate stats like ERA), fix the default sort fields, and polish column definitions for scope-appropriate display.

Purpose: Complete STAT-04 (batting table with sorting/filtering), STAT-05 (pitching table with sorting/filtering), and PAGE-05 (stats page integration) — the final functional requirements for Phase 2.
Output: Both hitting and pitching tables with correct default sorts, smart per-column sort direction, and scope-conditional columns.
</objective>

<execution_context>
@C:\Users\phill\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\phill\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-stats-engine/02-RESEARCH.md
@.planning/phases/02-stats-engine/02-CONTEXT.md
@.planning/phases/02-stats-engine/02-02-SUMMARY.md
@src/app/stats/players/page.tsx
@src/components/StatsTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add smart sort direction to column definitions and fix sort defaults</name>
  <files>src/app/stats/players/page.tsx</files>
  <action>
    Update the column definitions in page.tsx to include `sortDescFirst` metadata on every stat column, and fix the initial sort field.

    **Define ascending stats set** (rate stats where lower is better):
    ```typescript
    const ASCENDING_STATS = new Set(['era', 'whip', 'oppAvg', 'avg', 'obp', 'slg', 'ops'])
    ```

    Wait — AVG, OBP, SLG, OPS are "higher is better" stats. Only ERA, WHIP, and OPP AVG are "lower is better." Correct set:
    ```typescript
    const ASCENDING_STATS = new Set(['era', 'whip', 'oppAvg'])
    ```

    **Update hitting column definitions** in the `columns` useMemo. Add `sortDescFirst` to each column:
    - Player, Team: `sortDescFirst: true` (alphabetical convention, but these are string columns — TanStack handles them fine)
    - G, PA, AB, R, H, 2B, 3B, HR, RBI, BB, SO: `sortDescFirst: true` (counting stats — higher is better)
    - AVG, OBP, SLG, OPS: `sortDescFirst: true` (rate stats but higher is better in baseball)
    - wRC+, OPS+: `sortDescFirst: true` (higher is better)
    - Seasons (totals only): `sortDescFirst: true`

    **Update pitching column definitions.** Add `sortDescFirst` to each column:
    - Player, Team: `sortDescFirst: true`
    - G, IP, W, SV, K: `sortDescFirst: true` (counting stats — higher is better)
    - L: `sortDescFirst: true` (convention — you'd sort desc to see most losses, though losses are "bad")
    - BB, H, R, ER: `sortDescFirst: true` (counting stats)
    - ERA: `sortDescFirst: false` (lower is better)
    - WHIP: `sortDescFirst: false` (lower is better)
    - OPP AVG: `sortDescFirst: false` (lower is better)
    - Seasons (totals only): `sortDescFirst: true`

    **Fix the initialSort and initialSortDesc variables:**
    ```typescript
    const initialSort = tab === 'hitting' ? 'wrcPlus' : 'era'
    const initialSortDesc = tab === 'hitting' ? true : false
    ```
    Currently `initialSort` is `'ops'` for hitting — change to `'wrcPlus'`. The pitching sort is already correct (`'era'` with `initialSortDesc: false`).

    **Ensure scope-conditional columns are correct:**
    - Totals scope: Seasons column is spliced after Player (already done in current code, verify it's preserved)
    - Yearly scope: No Year column (already the case, verify it's preserved)
    - Totals scope: No Team column — totals data doesn't have meaningful team info (it's career totals). Remove the Team column from the totals column set, or keep it showing empty. Check the current behavior — if totals data has no team field, the column will show empty. It's acceptable to leave it.

    Actually, looking at the TotalsHittingRow interface: it has no `team` field. The Team column's accessorKey is `'team'` — this will render empty for totals. That's fine — keep it for layout consistency. If it looks awkward, a `cell` renderer can show "—" for totals. Use your judgment.

    **Keep the existing JSX structure for StatsTable rendering.** Just update the column definitions and initial sort props.
  </action>
  <verify>
    1. Run `npx tsc --noEmit` to confirm no type errors
    2. Navigate to /stats/players in the browser
    3. Default hitting view: verify table is sorted by wRC+ descending (highest wRC+ at top)
    4. Click HR column header — verify first click sorts descending (most HRs first)
    5. Click ERA column in pitching tab — verify first click sorts ascending (lowest ERA first)
    6. Switch to pitching tab — verify default sort is ERA ascending
    7. Switch to totals scope — verify Seasons column appears
  </verify>
  <done>Hitting defaults to wRC+ descending. Pitching defaults to ERA ascending. Every stat column has correct sortDescFirst metadata. Scope-conditional columns (Seasons) render correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure StatsTable respects sortDescFirst and verify full integration</name>
  <files>src/components/StatsTable.tsx</files>
  <action>
    Verify that StatsTable.tsx correctly passes column definitions (including `sortDescFirst`) through to TanStack React Table. TanStack's `useReactTable` natively supports `sortDescFirst` as a column-level property — no custom logic is needed.

    **Check current StatsTable implementation:**
    The component passes `columns` directly to `useReactTable({ columns, ... })`. TanStack reads `sortDescFirst` from each column definition automatically. This should work out of the box.

    **If sortDescFirst is NOT being respected** (test by clicking columns and observing sort direction):
    - Verify the column definitions in page.tsx are using the correct TanStack property name `sortDescFirst` (not `sortDescending` or any other name)
    - Verify the column definitions match TanStack's `ColumnDef` type signature

    **Add visual sort direction indicator enhancement** (optional but recommended):
    The current sort indicators show ↑ and ↓. Update the header rendering to also show a subtle unsorted indicator for clickable columns:
    ```tsx
    <span className="text-gray-400">
      {header.column.getIsSorted() === 'asc' ? '↑' :
       header.column.getIsSorted() === 'desc' ? '↓' :
       '⇅'}
    </span>
    ```
    This gives users a visual cue that columns are sortable. Use `'⇅'` or `'↕'` for the unsorted state — pick whichever looks better.

    **Verify the pagination page size is appropriate:**
    TanStack's default page size is 10. For stats tables showing a full season of players, 50 or "show all" would be better. Check current behavior — if pagination is cutting off players, consider setting `initialState: { pagination: { pageSize: 50 } }` in useReactTable options.

    **Final integration verification — test all combinations:**
    1. Hitting + Yearly + Year 2025 (or most recent) + Qualified ON → shows qualified hitters sorted by wRC+ desc
    2. Hitting + Yearly + Year 2025 + Team filter → shows only that team's hitters
    3. Hitting + Totals + Min PA 100 → shows career totals with Seasons column
    4. Pitching + Yearly + Qualified ON → shows qualified pitchers sorted by ERA asc
    5. Pitching + Totals + Min IP 75 → shows career pitching totals
    6. Search "Smith" → filters both tabs by name
    7. Select year → team dropdown populates correctly
    8. Empty state: select a team with very few players + high qualifier → shows "No players match these filters"
  </action>
  <verify>
    1. Run `npm run build` to confirm full build succeeds
    2. Navigate to /stats/players and test all 8 combinations listed above
    3. Verify clicking column headers sorts in the correct initial direction
    4. Verify the sort indicator (↑/↓) updates correctly on column clicks
    5. Verify pagination shows enough rows (at least 25-50 per page)
    6. Take a screenshot or note any visual issues for Phase 5 (design polish)
  </verify>
  <done>StatsTable respects sortDescFirst per column. All filter/sort/scope combinations work correctly. Both hitting and pitching tables display accurate, sortable, filterable data. Phase 2 success criteria are met.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify all Phase 2 success criteria:
1. Switching year, team, and scope filters updates stats tables correctly with no stale or missing data ✓
2. All-time batting totals pull from IH sheet (verified in Plan 02-01) ✓
3. Adding/reordering columns in Google Sheets does not break parsing — header-based mapping (Plan 02-01) ✓
4. Stats page displays sortable, filterable batting and pitching tables with all expected stat columns ✓
5. Default hitting sort: wRC+ descending ✓
6. Default pitching sort: ERA ascending ✓
7. Smart sort direction per column ✓
8. Sticky first 2 columns (Player, Team) — verify existing behavior preserved ✓
</verification>

<success_criteria>
- Default hitting sort is wRC+ descending
- Default pitching sort is ERA ascending
- Smart sort direction: counting stats sort descending first, rate stats (ERA/WHIP/OPP AVG) sort ascending first
- Totals view includes Seasons column
- All Phase 2 success criteria from ROADMAP.md are satisfied
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-stats-engine/02-03-SUMMARY.md`
</output>
