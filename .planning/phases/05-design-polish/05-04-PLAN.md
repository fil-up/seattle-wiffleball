---
phase: 05-design-polish
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - src/components/StatsTable.tsx
  - src/components/StatsFilter.tsx
  - src/app/stats/players/page.tsx
  - src/app/stats/players/[id]/page.tsx
  - src/app/teams/[id]/page.tsx
  - src/app/hall-of-fame/page.tsx
  - src/app/page.tsx
  - src/components/SiteNavigation.tsx
  - src/app/globals.css
  - src/components/PageNavigation.tsx (delete)
  - src/components/Navigation.tsx (delete)
autonomous: false

must_haves:
  truths:
    - "Stats tables show a small team logo next to each player's name"
    - "Player views display a generic silhouette headshot placeholder"
    - "Award icons appear next to player names where award data exists"
    - "Dropdown selectors for year, team, and stat category are visually polished with brand styling"
    - "Stats table headers stick to the top when scrolling down"
    - "Every page renders correctly on a 375px mobile screen with no horizontal body overflow"
    - "Mobile users see a hamburger menu that opens a slide-in navigation panel"
    - "Stats tables on mobile scroll horizontally with the player name column staying visible"
  artifacts:
    - path: "src/components/StatsTable.tsx"
      provides: "Polished stats table with sticky headers, theme-aware striping, team logo support"
      min_lines: 100
    - path: "src/components/StatsFilter.tsx"
      provides: "Branded dropdown selectors with consistent styling"
      min_lines: 40
  key_links:
    - from: "src/app/stats/players/page.tsx"
      to: "src/config/teamCodeLogos.ts"
      via: "resolveTeamByCode for inline team logos"
      pattern: "resolveTeamByCode"
    - from: "src/components/StatsTable.tsx"
      to: "sticky headers"
      via: "sticky top-0 z-30 class on thead"
      pattern: "sticky.*top-0"
---

<objective>
Apply final visual polish to stats tables (team logos, headshot placeholders, award icons, sticky headers, polished dropdowns) and ensure all pages are mobile responsive.

Purpose: This is the capstone of Phase 5 — the visual details that make the site feel like a professional sports stats hub (ESPN-inspired), and the mobile responsiveness that ensures usability at the field.
Output: Polished stats tables with inline team logos and icons, branded dropdowns, and a fully responsive site across all breakpoints.
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-design-polish/05-CONTEXT.md
@.planning/phases/05-design-polish/05-01-SUMMARY.md
@.planning/phases/05-design-polish/05-02-SUMMARY.md
@.planning/phases/05-design-polish/05-03-SUMMARY.md
@src/components/StatsTable.tsx
@src/components/StatsFilter.tsx
@src/config/teamCodeLogos.ts
@src/config/teams.ts
@src/app/stats/players/page.tsx
@src/app/stats/players/[id]/page.tsx
@src/app/teams/[id]/page.tsx
@src/app/hall-of-fame/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stats table visual polish — sticky headers, team logos, headshot placeholders, award icons, polished dropdowns</name>
  <files>src/components/StatsTable.tsx, src/components/StatsFilter.tsx, src/app/stats/players/page.tsx, src/app/stats/players/[id]/page.tsx, src/app/teams/[id]/page.tsx, src/app/hall-of-fame/page.tsx</files>
  <action>
    **0. Delete old navigation files (deferred from Plans 02/03):**
    - Delete src/components/PageNavigation.tsx (all imports removed by Plans 02 and 03)
    - Delete src/components/Navigation.tsx (stale/unused)

    **1. StatsTable.tsx visual overhaul:**

    Update the table styling for a professional ESPN-like appearance:

    - Sticky headers: Change `<thead>` to use `sticky top-0 z-30`. Header cells get `bg-brand-navy text-white` for a bold branded look. On scroll, headers stay pinned.
    - Row striping: Use `bg-surface-primary` for even rows and `bg-table-stripe` for odd rows (via theme tokens, automatic dark mode support).
    - Hover: `hover:bg-table-hover` on all rows.
    - Typography: Add `font-variant-numeric: tabular-nums` (via Tailwind `tabular-nums` class) to td cells containing numbers for aligned columns. Keep text-sm for data density.
    - Row spacing: `py-3` on cells (comfortable but not cramped).
    - Header text: `text-xs uppercase tracking-wider font-semibold` (already close to current, verify).
    - Sort indicators: Keep current ↑↓⇅ but style the active indicator with `text-brand-gold` to make it pop against the navy header.
    - Pagination: Update button styling to use brand tokens — `bg-brand-navy text-white` for active, `bg-surface-card border-border` for inactive. Page count text in `text-content-secondary`.
    - Sticky first columns: Ensure sticky column cells get explicit background color matching their row (even/odd) so they don't become transparent on scroll. Use the existing stickyFirstCols logic but ensure bg-inherit works with the new theme tokens — may need to explicitly set `bg-surface-primary` or `bg-table-stripe` based on row index.

    **2. StatsFilter.tsx dropdown restyle:**

    Replace default browser select styling with branded dropdowns:
    - Container: `bg-surface-card` with `border border-border rounded-lg p-4` and subtle shadow
    - Select elements: `bg-surface-primary border border-border rounded-md px-3 py-2 text-content-primary focus:ring-2 focus:ring-brand-gold focus:border-brand-gold outline-none transition-colors`
    - Labels: `text-sm font-semibold text-content-primary` (upgrade from text-gray-700)
    - Checkbox: `accent-brand-navy` or styled with `h-4 w-4 rounded border-border text-brand-navy focus:ring-brand-gold`
    - Overall: flex-wrap gap-4 layout maintained for responsive stacking

    **3. Inline team logos in stats columns:**

    In src/app/stats/players/page.tsx, update the "Player" column definition's cell renderer:
    - Import `{ resolveTeamByCode }` from '@/config/teamCodeLogos'
    - Import `Image` from 'next/image'
    - The cell renderer for the player name column should:
      - Look up the player's team code from the row data (the `team` or `teamCode` field)
      - Call `resolveTeamByCode(teamCode)` to get `logoUrl`
      - Render: small Image (16x16, unoptimized) of team logo next to the player name
      - If no logo found, skip the image (just show name)
      - Layout: `<div className="flex items-center gap-2">` with logo then name as Link

    Apply same pattern to any other page that shows player names with team context.

    **4. Headshot placeholder icon:**

    In src/app/stats/players/[id]/page.tsx (player detail page):
    - Add a generic silhouette headshot placeholder next to the player's name in the header area
    - Use an inline SVG silhouette icon (simple head-and-shoulders outline), sized 48x48 or 56x56
    - Style: `bg-surface-secondary rounded-full p-2 text-content-secondary`
    - This appears where a real headshot would go — ready to be swapped for actual images later

    **5. Award icons:**

    In src/app/hall-of-fame/page.tsx and src/app/stats/players/[id]/page.tsx:
    - Add small award icons inline next to player names where award data is available
    - MVP: small trophy SVG icon in brand-gold (⏏ or custom SVG, 16x16)
    - Batting title: star icon in brand-gold
    - If award data is null or empty (archives.json awards are currently null), degrade gracefully — show no icon, no error
    - Use a simple mapping: `{ 'MVP': TrophyIcon, 'Batting Title': StarIcon }` with inline SVG components
    - Icons should have a title attribute for hover tooltip explaining the award
  </action>
  <verify>
    - `npx next build` compiles without errors
    - StatsTable renders with navy branded headers and theme-aware striped rows
    - StatsFilter dropdowns have rounded borders and brand focus ring
    - Player name column in stats shows small team logo (or gracefully omits if missing)
    - Player detail page shows silhouette headshot placeholder
    - Award icons degrade gracefully when data is null
  </verify>
  <done>Stats tables have sticky branded headers, clean striping, team logos inline, and polished typography. Dropdowns use brand styling. Player views show headshot placeholders. Award icons appear where data exists and degrade gracefully where it doesn't.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile responsive pass across all pages</name>
  <files>src/components/StatsTable.tsx, src/components/SiteNavigation.tsx, src/app/page.tsx, src/app/teams/[id]/page.tsx, src/app/globals.css</files>
  <action>
    **1. Stats tables mobile behavior (StatsTable.tsx):**
    - The existing `overflow-x-auto` wrapper already enables horizontal scroll — verify it works
    - Make the first column (player name) sticky on mobile: when `stickyFirstCols >= 1`, ensure the sticky positioning works on mobile with proper z-index (z-20 for cells, z-30 for header intersection)
    - Ensure font size stays readable: minimum `text-sm` (14px) on mobile — do NOT shrink to text-xs
    - The sticky column cells must have an explicit background (not transparent) so content behind doesn't bleed through — set background to match row stripe color

    **2. Navigation mobile verification (SiteNavigation.tsx):**
    - Verify the hamburger → slide-in panel works correctly (this was built in 05-01)
    - Ensure the nav bar is sticky/fixed at the top on mobile: `sticky top-0 z-50` on the nav wrapper
    - Verify links are tappable with adequate touch targets (min 44px height)
    - Verify the panel closes when a link is tapped

    **3. Home page mobile stacking (src/app/page.tsx):**
    - Verify the hero section scales down properly on mobile (logo smaller, text wraps)
    - GameChanger widget section: full width on mobile
    - News + Standings: `grid-cols-1 lg:grid-cols-3` — on mobile, news and standings stack vertically (standings below news). Verify this already works.
    - Quick links: `grid-cols-1 md:grid-cols-2 lg:grid-cols-4` — 1 column on mobile, 2 on tablet, 4 on desktop
    - Videos grid: `grid-cols-1 md:grid-cols-3` — stack on mobile

    **4. Team detail mobile tabs (src/app/teams/[id]/page.tsx):**
    - On mobile (below md breakpoint), convert the team detail sections (roster, batting stats, pitching stats, team records) into a tabbed interface
    - Add a horizontal scrollable tab bar at the top of the content area: `<div className="flex overflow-x-auto border-b border-border md:hidden">`
    - Tabs: Roster | Batting | Pitching | Records
    - Each tab: `px-4 py-3 whitespace-nowrap font-medium` with active tab getting `border-b-2 border-brand-gold text-brand-navy`
    - On desktop (md+), show all sections stacked as they currently are
    - Use a state variable for active tab, only render the active section on mobile
    - This prevents the mobile page from being excessively long

    **5. Global responsive safeguards (src/app/globals.css):**
    - Add `html { overflow-x: hidden; }` to prevent horizontal body scroll on mobile (individual scrollable containers still work)
    - Ensure no fixed-width elements break the mobile viewport

    **6. Cross-page audit:**
    Systematically check these pages for mobile issues (fix any found):
    - Home: hero text wrapping, section stacking
    - Stats pages: table horizontal scroll, filter stacking
    - Teams index: card grid collapsing to single column
    - Leaderboards: podium layout on mobile
    - News: article cards stacking
    - Hall of Fame: player cards stacking
    - Archives: accordion on mobile
    - Schedule/Media: widget scaling
    - Rules/Info: prose max-width on mobile

    Common fixes:
    - Replace `px-6` with `px-4` on mobile (less side padding)
    - Ensure `container mx-auto` has `px-4` padding
    - Image containers use `relative w-full` not fixed widths
    - Text sizes: headings scale down (`text-2xl md:text-3xl lg:text-4xl`)
  </action>
  <verify>
    - `npx next build` compiles without errors
    - Open dev tools at 375px width: no horizontal scrollbar on any page body
    - Stats tables scroll horizontally with player name staying visible
    - Mobile nav hamburger opens slide-in panel
    - Team detail page shows tabs on mobile
    - Home page sections stack vertically on mobile
    - All text is readable (14px+ on mobile)
  </verify>
  <done>Every page renders correctly on mobile with no horizontal body overflow. Stats tables scroll horizontally with sticky name column. Mobile navigation works with slide-in panel. Team detail uses tabs on mobile. Home page stacks vertically.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 5 design and polish — theme system with dark/light mode, unified navigation, stats table visual polish with team logos and icons, skeleton loading states, error states, and mobile responsive layout across all pages.</what-built>
  <how-to-verify>
    1. Open the site at http://localhost:3000
    2. **Theme toggle**: Click the sun/moon icon in the nav — entire site should switch between light and dark mode
    3. **Navigation**: Verify consistent nav bar on every page with active page indicator
    4. **Mobile**: Open browser dev tools, set viewport to 375px width
       - Verify hamburger menu opens slide-in panel
       - Navigate to /stats/players — table scrolls horizontally, player name stays visible
       - Navigate to /teams/[any-team] — tabs appear for sections
       - Navigate to / — all sections stack vertically
    5. **Stats polish**: Navigate to /stats/players
       - Verify navy blue sticky headers
       - Verify striped rows with hover highlight
       - Verify small team logos next to player names
       - Verify polished dropdown selectors with gold focus ring
    6. **Player detail**: Navigate to /stats/players/[any-player]
       - Verify silhouette headshot placeholder
    7. **Loading states**: Throttle network to Slow 3G in dev tools
       - Reload /stats/players — verify skeleton table appears
       - Reload /news — verify skeleton cards appear
    8. **Error states**: Disconnect network (offline mode in dev tools)
       - Reload /stats/players — verify friendly error with retry button
    9. **Dark mode spot check**: Toggle to dark mode, visit 3-4 different pages — verify readability and no white flashes
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx next build` passes with zero errors
2. Lighthouse mobile audit on key pages — no critical layout shift or overflow issues
3. Manual walkthrough of all 18 pages in both light and dark mode
4. Mobile viewport (375px) test on home, stats, teams, leaderboards pages
5. Network throttle test for skeleton loading states
6. Offline test for error states with retry
</verification>

<success_criteria>
- Stats tables have branded sticky headers, striped rows, hover effects, and inline team logos
- Player views show headshot placeholder and award icons (graceful degradation)
- Dropdowns are visually polished with brand styling
- Every page works on 375px mobile without horizontal body scroll
- Mobile navigation hamburger → slide-in panel works
- Team detail uses tab interface on mobile
- All loading states show skeletons
- All error states show friendly retry UI
- Site passes visual verification checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-polish/05-04-SUMMARY.md`
</output>
