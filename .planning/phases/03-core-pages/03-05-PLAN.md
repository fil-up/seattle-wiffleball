---
phase: 03-core-pages
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/stats/players/[id]/page.tsx
  - src/app/leaderboards/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Clicking a player name on leaderboards navigates to their player detail page with career stats"
    - "Clicking a player card on team detail navigates to the player detail page"
    - "Player detail page shows career hitting stats by year in a table"
    - "Player detail page shows career pitching stats by year in a table (if player has pitching data)"
    - "Player detail page shows 404 message for invalid player IDs"
    - "Leaderboards year selector defaults to the most recent season, not all-time"
  artifacts:
    - path: "src/app/stats/players/[id]/page.tsx"
      provides: "Player detail view with career stats"
      min_lines: 80
    - path: "src/app/leaderboards/page.tsx"
      provides: "Leaderboards with correct year default"
  key_links:
    - from: "src/app/stats/players/[id]/page.tsx"
      to: "/api/players/[id]"
      via: "fetch in useEffect"
      pattern: "fetch.*api/players"
    - from: "src/components/LeaderboardPodium.tsx"
      to: "src/app/stats/players/[id]/page.tsx"
      via: "Next.js Link"
      pattern: "/stats/players/"
    - from: "src/app/leaderboards/page.tsx"
      to: "years[0]"
      via: "setYear after fetch"
      pattern: "setYear.*years\\[0\\]"
---

<objective>
Close 3 verification gaps from Phase 3 by creating the missing player detail page and fixing the leaderboards year default.

Purpose: Two gaps (player links from team detail + leaderboards = 404) share the same root cause — no page exists at `/stats/players/[id]`. The API route already works. A third gap is a one-line fix: leaderboards defaults to "all" instead of the current season.
Output: Working player detail page + correct leaderboards default year
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/api/players/[id]/route.ts
@src/app/stats/players/page.tsx
@src/app/leaderboards/page.tsx
@src/components/LeaderboardPodium.tsx
@src/components/PageNavigation.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create player detail page</name>
  <files>src/app/stats/players/[id]/page.tsx</files>
  <action>
Create a "use client" page at `src/app/stats/players/[id]/page.tsx` that displays a player's career hitting and pitching stats.

Follow the exact pattern from `src/app/stats/players/page.tsx` (PageNavigation at top, fetch in useEffect, stale banner, loading state).

**Data fetching:**
- Use `useParams()` to get `id` from the URL (same pattern as `src/app/stats/teams/[team]/page.tsx`)
- Fetch from `/api/players/${id}` in a useEffect triggered by `[id]`
- API returns `{ data: { hitting, pitching }, stale }` envelope
- Handle 404 response (player not found) with a user-friendly message

**Player name:**
- Extract from the first row of hitting or pitching data: `hitting[0]?.player?.name || pitching[0]?.player?.name`
- Display as page heading

**Layout — two stat tables (hitting then pitching), each shown only if the player has data for that type:**

Hitting table columns (one row per year):
- Year, Team, G, PA, AB, R, H, 2B, 3B, HR, RBI, BB, SO, AVG (.toFixed(3)), OBP (.toFixed(3)), SLG (.toFixed(3)), OPS (.toFixed(3)), wRC+ (.toFixed(0))

Pitching table columns (one row per year):
- Year, Team, G, IP (.toFixed(1)), W, L, SV, K, BB, H, R, ER, ERA (.toFixed(2)), WHIP (.toFixed(2)), OPP AVG (.toFixed(3)), K/9 (.toFixed(2))

Use simple HTML tables with Tailwind styling consistent with the team detail page (bg-white rounded-lg shadow, table headers bg-gray-50, striped rows). Sort rows by year descending.

Include a back link to `/stats/players` at the top of the content area.

**Important:** Do NOT use the StatsTable component — it's overkill for a simple career stats display. Use plain `<table>` elements like the yearly records table in `src/app/stats/teams/[team]/page.tsx`.
  </action>
  <verify>
Run `npx next build` — build should succeed with no errors.
Verify the file exists: `src/app/stats/players/[id]/page.tsx`
Manually check: visiting `/stats/players/any-valid-id` should render the page (not 404).
  </verify>
  <done>
Player detail page renders at /stats/players/[id] with career hitting and pitching stats by year. Links from LeaderboardPodium and team detail roster no longer 404. Page shows loading state, handles not-found players, and displays stale data banner when appropriate.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix leaderboards year default to current season</name>
  <files>src/app/leaderboards/page.tsx</files>
  <action>
In `src/app/leaderboards/page.tsx`, fix the year selector default so it starts on the most recent season instead of "all".

Current code (line 56): `const [year, setYear] = useState<string>('all')`

The first useEffect (lines 63-72) fetches years from `/api/stats` and stores them sorted descending in `years` state. After `setYears(ys)` completes, the year selector should default to the latest year.

Fix: Add a state flag or adjust the useEffect so that after years are fetched, `setYear(String(ys[0]))` is called — but only on initial load (not when years are re-fetched). The simplest approach:

1. Initialize year as empty string: `useState<string>('')`
2. In the years-fetching useEffect, after `setYears(ys)`, add: `setYear(String(ys[0]))` — this sets the initial year to the most recent season
3. The leaderboard data useEffect (lines 74-102) already triggers on `[year]` so it will re-fetch with the correct year
4. Keep "All-Time" as a selectable option in the dropdown — just don't default to it

Handle edge case: if `ys` is empty, fall back to 'all'.
  </action>
  <verify>
Run `npx next build` — no errors.
In the browser, visit `/leaderboards` — the year selector should show the most recent year (e.g., 2025) selected by default, not "All-Time".
Changing the selector to "All-Time" should still work and show all-time leaders with min 3 seasons.
  </verify>
  <done>
Leaderboards page loads with the most recent season selected by default. "All-Time" remains available as a dropdown option but is no longer the initial selection.
  </done>
</task>

</tasks>

<verification>
1. `npx next build` passes with no errors
2. Navigate to `/leaderboards` — year selector shows most recent year, not "All-Time"
3. Click any player name on leaderboards — navigates to `/stats/players/[id]` and shows career stats
4. Navigate to a team detail page → click a player in the roster → player detail page loads (not 404)
5. Visit `/stats/players/nonexistent-id` — shows player not found message (not crash)
</verification>

<success_criteria>
- Player detail page exists and renders career stats from the existing API
- All player links from leaderboards and team detail pages resolve (no more 404s)
- Leaderboards defaults to current season on initial load
- Both fixes pass `npx next build` without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-05-SUMMARY.md`
</output>
