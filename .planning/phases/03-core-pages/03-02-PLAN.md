---
phase: 03-core-pages
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/page.tsx
  - src/components/StandingsWidget.tsx
  - src/app/schedule/page.tsx
  - src/app/media/page.tsx
autonomous: true

must_haves:
  truths:
    - "Home page shows hero section, compact standings sidebar, news section, quick links, and compact GameChanger widget"
    - "Home page no longer has Featured Teams section (standings covers team visibility)"
    - "Schedule page displays full-size GameChanger scoreboard widget"
    - "Media page displays YouTube video gallery (or empty state if no channel ID configured)"
    - "All three pages use PageNavigation for consistent nav"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Restructured home page with standings and news layout"
      min_lines: 200
    - path: "src/components/StandingsWidget.tsx"
      provides: "Compact standings table component"
      exports: ["StandingsWidget"]
    - path: "src/app/schedule/page.tsx"
      provides: "Dedicated schedule page with full GC widget"
      min_lines: 20
    - path: "src/app/media/page.tsx"
      provides: "Media page with YouTube video gallery"
      min_lines: 30
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/GameChangerWidget.tsx"
      via: "import and render with maxGames=2"
      pattern: "GameChangerWidget"
    - from: "src/app/page.tsx"
      to: "src/components/StandingsWidget.tsx"
      via: "import and render in sidebar"
      pattern: "StandingsWidget"
    - from: "src/app/schedule/page.tsx"
      to: "src/components/GameChangerWidget.tsx"
      via: "import and render with large maxGames"
      pattern: "GameChangerWidget"
    - from: "src/app/media/page.tsx"
      to: "/api/youtube"
      via: "client-side fetch for video list"
      pattern: "fetch.*api/youtube"
---

<objective>
Restructure the home page layout and create the Schedule and Media pages. The home page gets a standings sidebar alongside news and uses shared components from plan 03-01. Schedule and Media are new top-level pages.

Purpose: Delivers the primary landing experience (home) and two new navigation destinations (Schedule, Media) that users expect from the nav.
Output: 3 page files (1 modified, 2 new) + 1 new widget component.
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages/03-RESEARCH.md
@.planning/phases/03-core-pages/03-01-SUMMARY.md

@src/app/page.tsx
@src/components/GameChangerWidget.tsx
@src/components/YouTubeEmbed.tsx
@src/app/api/youtube/route.ts
@src/app/api/standings/route.ts
@src/lib/news.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StandingsWidget and restructure home page</name>
  <files>src/components/StandingsWidget.tsx, src/app/page.tsx</files>
  <action>
**StandingsWidget.tsx:**

Create `src/components/StandingsWidget.tsx`:
1. `"use client"` directive.
2. Fetches from `/api/standings?scope=yearly` on mount.
3. Filters standings to the most recent year (latest year in the data).
4. Sorts by `pct` descending (win percentage).
5. Displays a compact table with columns: Rank, Team, W-L, PCT.
6. Limit to top 10 teams.
7. Style: compact card with `bg-white rounded-lg shadow p-4`, small text (`text-sm`).
8. Show "Standings" header with the year and a "Full Standings →" link to `/stats/teams`.
9. Handle loading state (skeleton or "Loading..." text).
10. Handle empty/error state gracefully.

**Restructure page.tsx (home page):**

The current home page (327 lines) has these sections in order:
1. Inline nav bar (lines 63-103)
2. Hero section (lines 105-169)
3. GameChanger widget section (lines 171-179)
4. Featured Teams section (lines 181-209) — REMOVE
5. Quick Links section (lines 211-253)
6. Featured News section (lines 255-324)

Transform to this layout:

1. **Keep the inline nav bar as-is** — it's the home page's custom nav with specific styling. Add Schedule (`/schedule`) and Media (`/media`) links to match PageNavigation's link set.
2. **Keep the hero section as-is** — it already has the GIF background, gradient, logo, title, and tagline per CONTEXT.md decisions.
3. **Replace the inline GameChangerWidget** with the shared `GameChangerWidget` component imported from `@/components/GameChangerWidget`. Use `maxGames={2}` for the compact home display. Keep the "Upcoming Games" heading.
4. **REMOVE the Featured Teams section entirely** — CONTEXT.md says "Featured teams section NOT needed (standings widget covers it)."
5. **Restructure news + standings** into a two-column layout below the GC widget:
   - Left column (wider, ~2/3): Featured news article card (large, with image) + 2-3 smaller recent article cards below it.
   - Right column (sidebar, ~1/3): StandingsWidget component.
   - On mobile: stack vertically (news first, then standings).
   - Keep the "Latest News" heading and "View All News →" link.
6. **Keep Quick Links section** at the bottom (already has Player Stats, Leaderboards, League Rules cards). Consider adding a Schedule quick link card.
7. **YouTube on home:** Below the news+standings section, add a "Latest Videos" section. Fetch from `/api/youtube?limit=3`. If data is empty (no channel ID yet), don't render the section at all. If videos exist, show 3 thumbnails using YouTubeEmbed in a 3-column grid.

The page stays `"use client"` for now — server-side splitting is Phase 5 optimization.
  </action>
  <verify>
- `npx tsc --noEmit` passes.
- Home page imports GameChangerWidget and StandingsWidget (not inline definitions).
- No "Featured Teams" section exists in the output.
- News + standings are in a two-column layout.
  </verify>
  <done>
- Home page shows: hero → compact GC widget → news (featured + recent) alongside standings sidebar → optional YouTube latest → quick links.
- StandingsWidget fetches live standings and shows ranked teams.
- Featured Teams section is removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Schedule and Media pages</name>
  <files>src/app/schedule/page.tsx, src/app/media/page.tsx</files>
  <action>
**Schedule page (`src/app/schedule/page.tsx`):**

1. `"use client"` directive (GameChangerWidget needs it).
2. Import `PageNavigation` and `GameChangerWidget` from `@/components/`.
3. Simple layout:
   - PageNavigation at top.
   - Container with heading "Schedule & Scores".
   - Brief description text: "View upcoming games, live scores, and recent results."
   - Full-width `GameChangerWidget` with `maxGames={10}` (show many games).
4. Wrap in the standard page structure: `min-h-screen bg-gray-50` → PageNavigation → content container.

**Media page (`src/app/media/page.tsx`):**

1. `"use client"` directive.
2. Import `PageNavigation` and `YouTubeEmbed` from `@/components/`.
3. On mount, fetch `/api/youtube?limit=12` to get videos.
4. Layout:
   - PageNavigation at top.
   - Container with heading "Media" and subheading "Game highlights, recaps, and league broadcasts."
   - If videos array is empty: show a friendly message — "No videos available yet. Check back soon!" with a muted icon.
   - If videos exist: render a responsive grid (`grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`). Each cell: `YouTubeEmbed` + video title below the embed.
5. Handle loading state.
6. Standard page structure: `min-h-screen bg-gray-50` → PageNavigation → content.
  </action>
  <verify>
- `npx tsc --noEmit` passes.
- Both new pages exist at the correct paths.
- Schedule page renders GameChangerWidget with large maxGames.
- Media page fetches from /api/youtube and handles empty state.
  </verify>
  <done>
- Navigating to /schedule shows the full GameChanger scoreboard.
- Navigating to /media shows a YouTube video gallery (or graceful empty state).
- Both pages use PageNavigation for consistent site nav.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors.
2. Home page no longer has inline GameChangerWidget function definition.
3. Home page no longer has Featured Teams section.
4. Home page has two-column news + standings layout.
5. /schedule and /media pages exist and render without errors.
6. `npm run build` completes without errors.
</verification>

<success_criteria>
- Home page matches the CONTEXT.md vision: hero → GC compact → news+standings → quick links.
- StandingsWidget shows current-year standings sorted by win percentage.
- Schedule page shows full GameChanger scoreboard for browsing game schedule.
- Media page handles both populated and empty YouTube states gracefully.
- All pages use PageNavigation (except home which has its own styled inline nav with matching links).
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-02-SUMMARY.md`
</output>
