---
phase: 01-data-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/stats/players/page.tsx
  - src/app/stats/teams/[team]/page.tsx
  - src/components/PlayerBattingData.tsx
  - src/components/PlayerPitchingData.tsx
  - src/components/SheetrockStandings.tsx
  - src/components/TeamMapping.tsx
  - src/components/StandingsDebug.tsx
autonomous: true

must_haves:
  truths:
    - "Every page that displays data fetches it through Next.js API routes — no client-side Google Sheets fetches"
    - "No component contains a docs.google.com URL or hardcoded spreadsheet ID"
    - "No component uses substring(47) or manual gviz parsing"
    - "Components display a stale data banner when the API indicates data is stale"
    - "The stats page correctly fetches hitting totals from the IH sheet (not IP)"
  artifacts:
    - path: "src/app/stats/players/page.tsx"
      provides: "Player stats page fetching from /api/players"
    - path: "src/app/stats/teams/[team]/page.tsx"
      provides: "Team detail page fetching from /api/teams and /api/standings"
    - path: "src/components/SheetrockStandings.tsx"
      provides: "Standings component fetching from /api/standings"
    - path: "src/components/PlayerBattingData.tsx"
      provides: "Batting data component fetching from /api/players"
    - path: "src/components/PlayerPitchingData.tsx"
      provides: "Pitching data component fetching from /api/players"
  key_links:
    - from: "src/app/stats/players/page.tsx"
      to: "/api/players"
      via: "fetch call with type and scope params"
      pattern: "fetch.*api/players"
    - from: "src/components/SheetrockStandings.tsx"
      to: "/api/standings"
      via: "fetch call with scope param"
      pattern: "fetch.*api/standings"
    - from: "src/components/PlayerBattingData.tsx"
      to: "/api/players"
      via: "fetch call with type=hitting"
      pattern: "fetch.*api/players"
    - from: "src/components/TeamMapping.tsx"
      to: "/api/teams"
      via: "fetch call"
      pattern: "fetch.*api/teams"
---

<objective>
Migrate every client component that currently fetches directly from Google Sheets (docs.google.com gviz URLs) to instead fetch from the internal Next.js API routes built in Plan 01-02. After this plan, no browser-side code touches Google Sheets directly — all data flows through the server-side pipeline with caching and staleness tracking.

Purpose: Complete the data pipeline by connecting the frontend to the new API routes, eliminating all client-side Google Sheets fetches.
Output: All 7 data-fetching components migrated to use API routes, with stale data banner support.
</objective>

<execution_context>
@C:\Users\phill\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\phill\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-02-SUMMARY.md
@src/app/stats/players/page.tsx
@src/app/stats/teams/[team]/page.tsx
@src/components/PlayerBattingData.tsx
@src/components/PlayerPitchingData.tsx
@src/components/SheetrockStandings.tsx
@src/components/TeamMapping.tsx
@src/components/StandingsDebug.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate stats pages to use API routes</name>
  <files>src/app/stats/players/page.tsx, src/app/stats/teams/[team]/page.tsx</files>
  <action>
    **`src/app/stats/players/page.tsx`** — The main stats page. Currently makes 4 different direct gviz fetches to Google Sheets with hardcoded spreadsheet ID and `substring(47)` parsing.

    Migration steps:
    1. Remove all direct Google Sheets URLs (the 4 gviz fetch URLs in the `fetchData` function)
    2. Remove the `substring(47).slice(0, -2)` parsing — the API routes handle this server-side
    3. Replace the `fetchData` function to call the internal API route instead:
       - `fetch('/api/players?type=${category}&scope=${isYearly ? "yearly" : "totals"}')`
       - Parse the response as `{ data, stale }` (the API route returns this shape)
       - Set stats from `response.data`
    4. The API route already returns data in the same shape that the current gviz transform produces (matching column indices), so the column definitions, filtering, and rendering logic should need minimal changes
    5. Add a `stale` state variable. When the API returns `stale: true`, show a banner at the top of the page:
       ```
       <div className="bg-yellow-50 border border-yellow-200 text-yellow-800 px-4 py-2 rounded mb-4 text-sm">
         Data may be outdated — we're having trouble reaching the latest stats. Showing last known data.
       </div>
       ```
    6. Remove the `console.log` statements (there are several debug logs in the current code)

    **BUG FIX — stats/players/page.tsx line 83**: The current code fetches batting all-time totals from the WRONG sheet tab:
    ```
    url = `...sheet=IP&range=C2:AL600`  // BUG: uses IP (pitching) tab for hitting totals
    ```
    This is STAT-02 (wrong sheet reference). By migrating to `/api/players?type=hitting&scope=totals`, this bug is automatically fixed because the API route (built in Plan 01-02) correctly fetches from the IH tab for hitting data.

    ---

    **`src/app/stats/teams/[team]/page.tsx`** — Team detail page. Currently makes 2 direct gviz fetches (team info + standings).

    Migration steps:
    1. Replace the `fetchTeamData` function:
       - Fetch from `/api/teams` to get the team list, then find the matching team by slug
       - Or fetch from `/api/teams/${teamSlug}` if the route supports it
    2. Replace the `fetchYearlyRecords` function:
       - Fetch from `/api/standings?scope=yearly` and filter for the current team
    3. Remove all direct Google Sheets URLs and `substring(47)` parsing
    4. Update the data shape handling to match the `{ data, stale }` API response format
    5. Add stale data banner (same pattern as above)
    6. The component also uses `PlayerBattingData` and `PlayerPitchingData` sub-components (migrated in Task 2), which pass `selectedYear` and team filters — ensure these props still flow correctly
  </action>
  <verify>
    1. No `docs.google.com` URLs remain in either file
    2. No `substring(47)` calls remain
    3. No hardcoded spreadsheet ID remains
    4. Both files fetch from `/api/...` internal routes
    5. `npm run build` succeeds
    6. Start dev server (`npm run dev`), navigate to `/stats/players` — data loads and displays correctly
    7. Navigate to a team page — team info and standings load correctly
  </verify>
  <done>
    Both stats pages fetch exclusively from API routes. No direct Google Sheets URLs. Stale data banner renders when API indicates stale data. The hitting totals bug (STAT-02) is fixed by virtue of using the correct API route.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate standalone data components to use API routes</name>
  <files>src/components/PlayerBattingData.tsx, src/components/PlayerPitchingData.tsx, src/components/SheetrockStandings.tsx, src/components/TeamMapping.tsx, src/components/StandingsDebug.tsx</files>
  <action>
    All 5 components follow the same migration pattern:
    1. Remove the direct Google Sheets gviz URL
    2. Remove `substring(47).slice(0, -2)` parsing
    3. Replace with a `fetch('/api/...')` call to the appropriate internal API route
    4. Parse the response as `{ data, stale }` and use `data` where the gviz-parsed data was used before
    5. Add stale data banner when `stale` is true

    **Component-specific details:**

    ---

    **`PlayerBattingData.tsx`**
    - Currently fetches: `IH!A700:AP2000` from Google Sheets directly
    - Replace with: `fetch('/api/players?type=hitting&scope=yearly')`
    - The API returns an array of player objects. The component currently transforms gviz rows into an object with keys like `Year`, `First`, `Last`, `Team`, `AVG`, etc. The API route already returns transformed data, so simplify the component to use the API response directly.
    - The component's filtering logic (by selectedYear, selectedTeam, selectedPlayer, qualifierOnly) should continue to work on the API response data — just filter the `data` array from the API response.
    - Add stale banner.

    ---

    **`PlayerPitchingData.tsx`**
    - Currently fetches: `IP!A300:AA999` from Google Sheets directly
    - Replace with: `fetch('/api/players?type=pitching&scope=yearly')`
    - Same migration pattern as PlayerBattingData above.
    - Add stale banner.

    ---

    **`SheetrockStandings.tsx`**
    - Currently makes 2 direct gviz fetches:
      - Yearly standings: `Standings!A54:T952`
      - All-time standings: `Standings!B1:T49`
    - Replace yearly with: `fetch('/api/standings?scope=yearly')`
    - Replace all-time with: `fetch('/api/standings?scope=alltime')`
    - The API returns `{ data, stale }`. The component's rendering logic (yearly tab with year selector, all-time tab with sorted teams) should work the same — just use the `data` array from the API response.
    - The component extracts unique years from the data for the year selector — this should continue to work on the API response data.
    - Add stale banner.

    ---

    **`TeamMapping.tsx`**
    - Currently fetches: `Player/Team Adj!O4:S100` from Google Sheets directly
    - Replace with: `fetch('/api/teams')`
    - The API route already transforms this data into team objects with `{ id, name, abbreviation, uniqueTeamName, teamCode, logoUrl }`.
    - The component maps rows to a similar shape — simplify to use the API response directly.
    - Add stale banner.

    ---

    **`StandingsDebug.tsx`**
    - Currently fetches Standings with a variable range from Google Sheets directly
    - This is a debug/diagnostic component. Migrate it to use `/api/standings` with the appropriate scope parameter.
    - If the debug component needs variable range access for diagnostics, it can use query params like `/api/standings?scope=yearly` or `/api/standings?scope=alltime`.
    - Add stale banner.

    ---

    **Stale data banner pattern (use across all components):**
    When the API response has `stale: true`, render a small yellow banner above the data:
    ```tsx
    {stale && (
      <div className="bg-yellow-50 border border-yellow-200 text-yellow-800 px-4 py-2 rounded mb-4 text-sm">
        Data may be outdated — showing last known data while we reconnect.
      </div>
    )}
    ```

    Position this banner inside the component, above the data table/content, below any headers or navigation.
  </action>
  <verify>
    1. No `docs.google.com` URLs remain in any of the 5 component files
    2. No `substring(47)` calls remain in any component
    3. No hardcoded spreadsheet ID in any component
    4. All components fetch from `/api/...` routes
    5. `npm run build` succeeds
    6. Run a codebase-wide search for the spreadsheet ID (`1bkQNmqFBqBqyqwo5vtNZHSDYTwvU1_WSsfE2GlBd3Ek`) — it should appear ONLY in `.env.local` and nowhere in source code
    7. Run a codebase-wide search for `substring(47)` — should return zero results
    8. Run a codebase-wide search for `docs.google.com/spreadsheets` — should return zero results in source code
  </verify>
  <done>
    All 5 standalone components fetch from internal API routes. Zero direct Google Sheets fetches remain in the entire codebase. Stale data banners render when API returns stale data. No hardcoded spreadsheet IDs or fragile parsing in any source file.
  </done>
</task>

</tasks>

<verification>
After both tasks complete — these are the Phase 1 success criteria:
1. **Every page that displays data fetches it through Next.js API routes** — no client-side Google Sheets fetches
   - Verify: `grep -r "docs.google.com/spreadsheets" src/` returns zero results
2. **The spreadsheet ID is configured in one environment variable** — changing it updates the entire site
   - Verify: `grep -r "1bkQNmqFBqBqyqwo5vtNZHSDYTwvU1_WSsfE2GlBd3Ek" src/` returns zero results; only `.env.local` has it
3. **Google Sheets gviz responses are parsed by a single shared utility** used across all API routes
   - Verify: `grep -r "substring(47)" src/` returns zero results; only `src/lib/sheets.ts` has `parseGvizResponse`
4. **Repeated page loads within the cache window do not trigger new requests to Google Sheets**
   - Verify: Load a page twice within 30 seconds, confirm (via Network tab or server logs) only one request goes to Google Sheets
5. **No Prisma schema, SQLite database file, or Prisma client code exists in the codebase**
   - Verify: Already confirmed in Plan 01-01
</verification>

<success_criteria>
- Zero direct Google Sheets fetches from any client component
- Zero hardcoded spreadsheet IDs in source code
- Zero uses of fragile substring(47) parsing
- All data-displaying components use internal API routes
- Stale data banner appears when API indicates stale data
- The entire data pipeline is: Google Sheets → sheets.ts (cache) → API routes → client components
- npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-03-SUMMARY.md`
</output>
