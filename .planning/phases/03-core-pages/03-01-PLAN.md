---
phase: 03-core-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PageNavigation.tsx
  - src/components/GameChangerWidget.tsx
  - src/components/YouTubeEmbed.tsx
  - src/app/api/youtube/route.ts
autonomous: true

must_haves:
  truths:
    - "Desktop nav shows Home, News, Players, Teams, Leaderboards, Schedule, and Media links"
    - "Mobile hamburger menu opens and closes, showing all nav links"
    - "GameChangerWidget component accepts maxGames prop and renders GC scoreboard"
    - "YouTubeEmbed component renders a lite YouTube embed that loads iframe only on click"
    - "YouTube API route returns video data from RSS feed in { data, stale } envelope"
  artifacts:
    - path: "src/components/PageNavigation.tsx"
      provides: "Fixed navigation with Schedule/Media links and working mobile menu"
      contains: "Schedule"
    - path: "src/components/GameChangerWidget.tsx"
      provides: "Reusable GameChanger scoreboard widget"
      exports: ["GameChangerWidget"]
    - path: "src/components/YouTubeEmbed.tsx"
      provides: "Lite YouTube embed wrapper"
      exports: ["YouTubeEmbed"]
    - path: "src/app/api/youtube/route.ts"
      provides: "Server-side YouTube RSS feed fetcher"
      exports: ["GET"]
  key_links:
    - from: "src/components/GameChangerWidget.tsx"
      to: "GC SDK (widgets.gc.com)"
      via: "dynamic script load in useEffect"
      pattern: "gc\\.scoreboard\\.init"
    - from: "src/components/YouTubeEmbed.tsx"
      to: "react-lite-youtube-embed"
      via: "LiteYouTubeEmbed component import"
      pattern: "LiteYouTubeEmbed"
    - from: "src/app/api/youtube/route.ts"
      to: "YouTube RSS feed"
      via: "server-side fetch"
      pattern: "youtube\\.com/feeds/videos\\.xml"
---

<objective>
Create shared reusable components (GameChangerWidget, YouTubeEmbed) and fix PageNavigation so all subsequent page-building plans have the building blocks they need.

Purpose: Pages in plans 03-02 through 03-04 import these shared components. Building them first in a focused plan avoids duplication and keeps page plans clean.
Output: 4 new/modified component files ready for import, react-lite-youtube-embed installed.
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages/03-RESEARCH.md

@src/components/PageNavigation.tsx
@src/app/page.tsx (lines 9-54 — the inline GameChangerWidget to extract)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PageNavigation and extract GameChangerWidget</name>
  <files>src/components/PageNavigation.tsx, src/components/GameChangerWidget.tsx</files>
  <action>
**PageNavigation.tsx — fix mobile menu and add nav links:**

1. Add `"use client"` directive at top (needed for useState).
2. Add `useState<boolean>(false)` for `menuOpen`.
3. Wire the existing hamburger button `onClick` to toggle `menuOpen`.
4. Change the mobile menu container from `className="hidden md:hidden"` (line 65 — this is the bug, it's always hidden) to `className={menuOpen ? 'block md:hidden' : 'hidden'}`.
5. Add two new nav links to BOTH the desktop `hidden md:block` section AND the mobile menu:
   - Schedule → `/schedule` (use a calendar SVG icon)
   - Media → `/media` (use a video/play SVG icon)
6. Add Schedule and Media links in mobile menu section too (currently missing News from mobile — add all 7 links: Home, News, Players, Teams, Leaderboards, Schedule, Media).
7. When mobile menu is open, change the hamburger icon to an X (close icon). Use conditional rendering based on `menuOpen`.
8. Close mobile menu on link click by setting `menuOpen(false)` — wrap each mobile Link in an onClick handler or use a wrapper.

**GameChangerWidget.tsx — extract to shared component:**

Create `src/components/GameChangerWidget.tsx` based on the inline `GameChangerWidget` function currently in `src/app/page.tsx` (lines 9-54). Key changes from the inline version:

1. Add `"use client"` directive.
2. Accept props: `{ maxGames?: number; className?: string }` with default `maxGames = 4`.
3. Use a unique target ID per instance: generate with `useId()` or accept as prop. This prevents conflicts if multiple widgets exist across pages. Use `gc-scoreboard-${useId()}` pattern but note useId returns colons — sanitize or use `useRef` to generate a stable ID like `gc-widget-${Math.random().toString(36).slice(2, 8)}`.
4. Check `window.GC` existence before loading script to prevent duplicate SDK loads.
5. Remove `console.log`/`console.error` calls (cleanup for production).
6. Keep the loading placeholder div.
7. Export as named export: `export function GameChangerWidget(...)`.

Do NOT modify `src/app/page.tsx` yet — that happens in plan 03-02.
  </action>
  <verify>
- `npx tsc --noEmit` passes with no errors in the new/modified files.
- PageNavigation.tsx imports useState from react and has "use client" directive.
- GameChangerWidget.tsx exists with maxGames prop and "use client" directive.
  </verify>
  <done>
- PageNavigation has 7 nav links (including Schedule, Media) in both desktop and mobile sections.
- Mobile menu toggles open/closed via hamburger button.
- GameChangerWidget is a standalone component with configurable maxGames.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install react-lite-youtube-embed, create YouTubeEmbed and YouTube API route</name>
  <files>src/components/YouTubeEmbed.tsx, src/app/api/youtube/route.ts</files>
  <action>
**Install dependency:**
```bash
npm install react-lite-youtube-embed
```

**YouTubeEmbed.tsx:**

Create `src/components/YouTubeEmbed.tsx`:
1. `"use client"` directive.
2. Import `LiteYouTubeEmbed` from `react-lite-youtube-embed`.
3. Import the CSS: `import 'react-lite-youtube-embed/dist/LiteYouTubeEmbed.css'`.
4. Props: `{ videoId: string; title: string; className?: string }`.
5. Render `<LiteYouTubeEmbed id={videoId} title={title} poster="hqdefault" webp />`.
6. Wrap in a div with `className` prop and `rounded-lg overflow-hidden` base styles.
7. Export as named export.

**YouTube API route — `/api/youtube`:**

Create `src/app/api/youtube/route.ts`:
1. Define a placeholder `CHANNEL_ID` constant — set to empty string with a comment `// TODO: Replace with Seattle Wiffle YouTube channel ID`.
2. Build RSS URL: `https://www.youtube.com/feeds/videos.xml?channel_id=${CHANNEL_ID}`.
3. GET handler:
   - If `CHANNEL_ID` is empty, return `{ data: [], stale: false }` (graceful degradation).
   - Otherwise fetch the RSS URL with `{ next: { revalidate: 3600 } }` (1hr cache).
   - Parse XML using regex (the feed is simple/stable):
     - Extract `<entry>` blocks.
     - From each entry: `<yt:videoId>`, `<title>`, `<published>`.
   - Return `{ data: videos.slice(0, 12), stale: false }` on success.
   - Return `{ data: [], stale: true }` on fetch error.
4. Accept optional `?limit=N` query param (default 12).
5. Return `{ data, stale }` envelope (matches project convention).
  </action>
  <verify>
- `npm ls react-lite-youtube-embed` shows the package installed.
- `npx tsc --noEmit` passes.
- YouTubeEmbed.tsx imports from react-lite-youtube-embed successfully.
- YouTube API route exports a GET function.
  </verify>
  <done>
- react-lite-youtube-embed is in package.json dependencies.
- YouTubeEmbed component renders a lite embed with videoId and title props.
- /api/youtube route returns { data, stale } with graceful empty-array fallback when channel ID is missing.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors.
2. All 4 files exist: PageNavigation.tsx (modified), GameChangerWidget.tsx (new), YouTubeEmbed.tsx (new), api/youtube/route.ts (new).
3. `npm ls react-lite-youtube-embed` confirms installation.
4. PageNavigation mobile menu uses state toggle (not hardcoded `hidden`).
</verification>

<success_criteria>
- Shared components are importable by plan 03-02 (home/schedule/media pages).
- Navigation has Schedule and Media links on desktop and mobile.
- Mobile menu bug is fixed (opens and closes).
- GameChangerWidget supports compact (maxGames=2) and full (maxGames=10+) modes.
- YouTubeEmbed wraps react-lite-youtube-embed with project-consistent styling.
- YouTube API route gracefully handles missing channel ID.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-01-SUMMARY.md`
</output>
