---
phase: 05-design-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/globals.css
  - tailwind.config.ts
  - src/app/layout.tsx
  - src/components/SiteNavigation.tsx
  - src/components/ThemeToggle.tsx
  - src/components/ErrorState.tsx
  - src/components/Skeleton.tsx
autonomous: true

must_haves:
  truths:
    - "Toggling the theme switch changes the entire site between light and dark mode"
    - "Every page shows the same navigation bar at the top with league logo and links"
    - "Mobile users see a hamburger icon that opens a slide-in navigation panel"
    - "Active page is visually indicated in the navigation bar"
    - "ErrorState and Skeleton components exist and render correctly in both themes"
  artifacts:
    - path: "src/components/SiteNavigation.tsx"
      provides: "Unified navigation with desktop and mobile layouts"
      min_lines: 80
    - path: "src/components/ThemeToggle.tsx"
      provides: "Dark/light mode toggle button"
      min_lines: 15
    - path: "src/components/ErrorState.tsx"
      provides: "Friendly error message with retry button"
      min_lines: 20
    - path: "src/components/Skeleton.tsx"
      provides: "TableSkeleton and CardSkeleton loading placeholders"
      min_lines: 30
    - path: "src/app/globals.css"
      provides: "CSS custom properties for light and dark theme colors"
      contains: ":root"
    - path: "tailwind.config.ts"
      provides: "Semantic color tokens mapped to CSS variables"
      contains: "darkMode"
  key_links:
    - from: "src/app/layout.tsx"
      to: "ThemeProvider"
      via: "next-themes wrapper"
      pattern: "ThemeProvider"
    - from: "src/app/layout.tsx"
      to: "src/components/SiteNavigation.tsx"
      via: "component render above main"
      pattern: "<SiteNavigation"
    - from: "tailwind.config.ts"
      to: "src/app/globals.css"
      via: "CSS custom properties referenced by Tailwind tokens"
      pattern: "var\\(--color-"
---

<objective>
Set up the design system foundation: color theme with CSS custom properties, dark/light mode toggle, unified navigation component, and shared UI components (error states, skeletons).

Purpose: Everything in Phase 5 builds on this foundation — theme tokens, navigation, and shared components are prerequisites for the page-by-page migration and visual polish that follow.
Output: Working theme toggle, unified navigation in layout.tsx, reusable ErrorState and Skeleton components.
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-design-polish/05-CONTEXT.md
@.planning/phases/05-design-polish/05-RESEARCH.md
@tailwind.config.ts
@src/app/globals.css
@src/app/layout.tsx
@src/components/PageNavigation.tsx
@src/config/teams.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme system — dependencies, CSS variables, Tailwind config, ThemeProvider</name>
  <files>package.json, src/app/globals.css, tailwind.config.ts, src/app/layout.tsx</files>
  <action>
    1. Install dependencies: `npm install next-themes @headlessui/react clsx`

    2. Replace src/app/globals.css with CSS custom properties for the theme. Define color values as space-separated RGB channels so Tailwind can apply opacity modifiers:

    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    :root {
      --color-bg-primary: 255 255 255;
      --color-bg-secondary: 249 250 251;
      --color-bg-card: 255 255 255;
      --color-text-primary: 17 24 39;
      --color-text-secondary: 107 114 128;
      --color-brand-primary: 37 57 123;
      --color-brand-accent: 255 215 0;
      --color-table-stripe: 249 250 251;
      --color-table-hover: 243 244 246;
      --color-border: 229 231 235;
    }

    .dark {
      --color-bg-primary: 17 24 39;
      --color-bg-secondary: 31 41 55;
      --color-bg-card: 31 41 55;
      --color-text-primary: 243 244 246;
      --color-text-secondary: 156 163 175;
      --color-brand-primary: 59 91 173;
      --color-brand-accent: 255 215 0;
      --color-table-stripe: 31 41 55;
      --color-table-hover: 55 65 81;
      --color-border: 55 65 81;
    }
    ```

    3. Update tailwind.config.ts: add `darkMode: 'class'` at the top level. Extend the colors object with semantic tokens that reference the CSS variables. Keep the existing @tailwindcss/typography plugin. Remove the old hardcoded `primary`/`secondary` colors (replaced by brand tokens):

    ```typescript
    darkMode: 'class',
    theme: {
      extend: {
        colors: {
          brand: {
            navy: 'rgb(var(--color-brand-primary) / <alpha-value>)',
            gold: 'rgb(var(--color-brand-accent) / <alpha-value>)',
          },
          surface: {
            primary: 'rgb(var(--color-bg-primary) / <alpha-value>)',
            secondary: 'rgb(var(--color-bg-secondary) / <alpha-value>)',
            card: 'rgb(var(--color-bg-card) / <alpha-value>)',
          },
          content: {
            primary: 'rgb(var(--color-text-primary) / <alpha-value>)',
            secondary: 'rgb(var(--color-text-secondary) / <alpha-value>)',
          },
          'table-stripe': 'rgb(var(--color-table-stripe) / <alpha-value>)',
          'table-hover': 'rgb(var(--color-table-hover) / <alpha-value>)',
          border: {
            DEFAULT: 'rgb(var(--color-border) / <alpha-value>)',
          },
        },
      },
    },
    ```

    4. Update src/app/layout.tsx:
    - Import ThemeProvider from 'next-themes'
    - Add `suppressHydrationWarning` to the `<html>` tag
    - Wrap body contents in `<ThemeProvider attribute="class" defaultTheme="light" enableSystem>`
    - Change `<body>` className to use theme token: `${inter.className} bg-surface-primary text-content-primary`
    - Do NOT add SiteNavigation yet (Task 2)
    - Keep existing footer for now (will restyle in Task 2)
  </action>
  <verify>
    - `npm ls next-themes @headlessui/react clsx` shows all three installed
    - `npx next build` compiles without errors
    - globals.css contains :root and .dark blocks with CSS custom properties
    - tailwind.config.ts has darkMode: 'class' and brand/surface/content color tokens
    - layout.tsx wraps children in ThemeProvider
  </verify>
  <done>Theme system is configured: CSS variables define light/dark colors, Tailwind tokens reference them, and ThemeProvider enables class-based dark mode switching.</done>
</task>

<task type="auto">
  <name>Task 2: Unified SiteNavigation with theme toggle and footer restyle</name>
  <files>src/components/SiteNavigation.tsx, src/components/ThemeToggle.tsx, src/app/layout.tsx</files>
  <action>
    1. Create src/components/ThemeToggle.tsx:
    - "use client" component
    - Uses `useTheme()` from next-themes
    - Uses a `mounted` state flag (set true in useEffect) to avoid hydration mismatch
    - Renders a button with sun icon (light mode) or moon icon (dark mode)
    - Styled with text-content-secondary hover:text-brand-gold transition
    - On click: toggles between 'light' and 'dark' via setTheme

    2. Create src/components/SiteNavigation.tsx:
    - "use client" component
    - Uses `usePathname()` from next/navigation for active link detection
    - Uses `useState` for mobile menu open/close
    - Uses `Dialog` and `DialogPanel` from @headlessui/react for mobile slide-in panel

    Navigation links array (label + href):
    - Home: /
    - News: /news
    - Players: /stats/players
    - Teams: /teams
    - Leaderboards: /leaderboards
    - Schedule: /schedule
    - Media: /media

    Secondary links (shown in mobile menu and desktop "More" dropdown or just in mobile):
    - Rules: /rules
    - Archives: /archives
    - Info: /info
    - Hall of Fame: /hall-of-fame

    Desktop layout (hidden on mobile):
    - Full-width bg-brand-navy bar
    - Left: league logo (Next.js Image from /images/seattle-wiffleball-logo.png, 36x36) + "Seattle Wiffle" text in white font-bold
    - Center/right: horizontal nav links in text-white, spaced with gap-1
    - Active link: border-b-2 border-brand-gold (use pathname matching — exact match for / and startsWith for others)
    - Hover: text-brand-gold transition-colors
    - Far right: ThemeToggle component
    - Below the nav bar: 3px gold accent line (bg-brand-gold h-[3px])

    Mobile layout (hidden on desktop, md:hidden):
    - Same bg-brand-navy bar with logo on left and hamburger icon + ThemeToggle on right
    - Hamburger button: three horizontal lines SVG, text-white
    - Opens Headless UI Dialog with:
      - Semi-transparent overlay (bg-black/50)
      - Panel sliding in from right (translate-x animation via Transition)
      - Panel: bg-surface-primary, full height, w-72 max
      - Close button (X icon) at top right of panel
      - All nav links (primary + secondary) as block links with py-3 border-b border-border
      - Active link has text-brand-navy font-bold (light) or text-brand-gold (dark)
    - Clicking a link closes the menu (setOpen(false))

    3. Update src/app/layout.tsx:
    - Import SiteNavigation
    - Add `<SiteNavigation />` inside ThemeProvider, before `<main>`
    - Restyle the footer to use theme tokens:
      - bg-gray-900 → bg-gray-900 dark:bg-gray-950 (footer stays dark in both modes for ESPN feel)
      - text-gray-400 → text-gray-400
      - border-gray-800 → border-gray-800
      - Footer stays dark-themed in both modes (convention for sports sites)
    - Add `min-h-screen` and `flex flex-col` to body for sticky footer
    - Add `flex-1` to `<main>`
  </action>
  <verify>
    - `npx next build` compiles without errors
    - SiteNavigation.tsx exists with desktop and mobile layouts
    - ThemeToggle.tsx exists and uses next-themes
    - layout.tsx renders SiteNavigation above main content
    - Navigation shows league logo, all links, and theme toggle on desktop
  </verify>
  <done>Unified navigation renders on every page from layout.tsx. Desktop shows horizontal link bar with active indicator and theme toggle. Mobile shows hamburger that opens slide-in panel. Footer is styled consistently.</done>
</task>

<task type="auto">
  <name>Task 3: Shared ErrorState and Skeleton loading components</name>
  <files>src/components/ErrorState.tsx, src/components/Skeleton.tsx</files>
  <action>
    1. Create src/components/ErrorState.tsx:
    - Props: `{ message?: string; onRetry?: () => void }`
    - Default message: "Couldn't grab the stats. Try again?"
    - Layout: centered flex column with:
      - Warning/error icon (SVG circle with exclamation mark, text-brand-gold, w-12 h-12)
      - Message text in text-content-primary font-medium text-lg
      - Retry button (if onRetry provided): bg-brand-navy text-white px-6 py-2 rounded-lg hover:bg-brand-navy/90 transition-colors, text "Try Again"
    - Entire component wrapped in a padded container (py-12) with text-center
    - Works in both light and dark mode via theme tokens

    2. Create src/components/Skeleton.tsx:
    - Export multiple skeleton components from a single file:

    TableSkeleton (props: { rows?: number, cols?: number }):
    - Default: 8 rows, 6 cols
    - Renders a div matching table layout:
      - Header row: cols number of rounded rectangles (h-4 w-full) with bg-surface-secondary animate-pulse
      - Body rows: each row has cols number of rounded rectangles with varying widths (first col wider for names)
      - Alternating row backgrounds using table-stripe token
      - Container has rounded-lg overflow-hidden border border-border

    CardSkeleton:
    - Rounded-lg container with bg-surface-card border border-border
    - Inside: rectangle for image area (h-40 bg-surface-secondary animate-pulse rounded)
    - Below: 3 lines of varying widths (h-4, h-3, h-3) for title/text placeholders
    - Padding p-4 with space-y-3

    Both components use theme tokens so they work in light and dark mode automatically.
  </action>
  <verify>
    - Both files exist and export their components
    - `npx next build` compiles without errors
    - Components use theme tokens (bg-surface-*, text-content-*, border-border)
    - Skeleton components have animate-pulse for loading animation
  </verify>
  <done>ErrorState renders a friendly error message with optional retry button. TableSkeleton and CardSkeleton render animated loading placeholders. All components support light and dark themes.</done>
</task>

</tasks>

<verification>
1. Run `npx next build` — should compile with zero errors
2. Run `npx next dev` and verify:
   - Navigation bar appears at top of every page (check / and /stats/players)
   - Theme toggle switches between light and dark mode
   - Mobile viewport (375px) shows hamburger icon that opens slide-in menu
   - Active page link is visually distinct in the nav
3. Verify globals.css has :root and .dark CSS custom property blocks
4. Verify tailwind.config.ts has darkMode: 'class' and semantic color tokens
</verification>

<success_criteria>
- Theme toggle switches the entire site between light and dark mode without page reload
- SiteNavigation renders consistently on all pages via layout.tsx
- Desktop nav shows logo, links with active indicator, and theme toggle
- Mobile nav shows hamburger → slide-in panel with all links
- ErrorState and Skeleton components exist and render in both themes
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-polish/05-01-SUMMARY.md`
</output>
