---
phase: 06-infrastructure-launch
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: []
autonomous: false

user_setup:
  - service: vercel
    why: "Next.js hosting and deployment"
    env_vars:
      - name: SPREADSHEET_ID
        source: "The Google Sheets ID already in your .env.local file"
    dashboard_config:
      - task: "Import GitHub repository as new Vercel project"
        location: "vercel.com -> New Project -> Import Git Repository"
      - task: "Add SPREADSHEET_ID environment variable"
        location: "Vercel Project -> Settings -> Environment Variables"

must_haves:
  truths:
    - "Site is live on a public Vercel URL"
    - "All data-fetching pages display real data (not error states)"
    - "SPREADSHEET_ID is set in Vercel environment variables"
  artifacts: []
  key_links:
    - from: "Vercel environment"
      to: "src/lib/sheets.ts"
      via: "process.env.SPREADSHEET_ID"
      pattern: "SPREADSHEET_ID"
---

<objective>
Deploy the site to Vercel with correct environment configuration and verify it serves live data.

Purpose: Satisfy INFRA-01 — site is live and accessible.
Output: A deployed, functional site on Vercel.
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infrastructure-launch/06-RESEARCH.md
@.planning/phases/06-infrastructure-launch/06-01-SUMMARY.md
@src/lib/sheets.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit cleanup changes and push to GitHub</name>
  <files></files>
  <action>
    Ensure all changes from plan 06-01 (debug deletions, rename) are committed and pushed to the main branch on GitHub. The Vercel deployment will pull from this branch.

    Run `git status` to check for uncommitted changes. If any exist from plan 06-01, commit them with message "chore(06): remove debug routes and rename SheetrockStandings" and push to origin main.

    If changes are already committed and pushed, skip this step.
  </action>
  <verify>Run: git status — working tree should be clean. Run: git log --oneline -1 — should show the cleanup commit.</verify>
  <done>All cleanup changes are committed and pushed to GitHub main branch.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy to Vercel</name>
  <action>
    The user needs to deploy the site via the Vercel dashboard. Claude cannot authenticate to Vercel on their behalf.

    Steps for the user:
    1. Go to https://vercel.com and sign in (or create account)
    2. Click "Add New..." -> "Project"
    3. Import the seattle-wiffleball GitHub repository
    4. Vercel will auto-detect Next.js — no build settings changes needed
    5. Before clicking "Deploy", expand "Environment Variables"
    6. Add: Key = `SPREADSHEET_ID`, Value = the Google Sheets ID from your `.env.local` file
    7. Set environments: Production (checked), Preview (checked)
    8. Click "Deploy"
    9. Wait for build to complete
    10. Share the deployed URL (e.g., seattle-wiffleball.vercel.app)
  </action>
  <resume-signal>Paste the deployed Vercel URL, or describe any issues encountered.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full site deployment on Vercel with live data</what-built>
  <how-to-verify>
    Visit the deployed Vercel URL and verify:
    1. Home page loads with standings widget showing team data
    2. Navigate to Stats page — batting/pitching tables display real data
    3. Navigate to Teams page — team cards show with logos and records
    4. Navigate to Leaderboards — top performers display correctly
    5. Navigate to News — articles list renders
    6. Try /debug/player-data — should return 404 (not found)
    7. Check on mobile phone — responsive layout works
  </how-to-verify>
  <resume-signal>Type "approved" if all pages show live data, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- Site is accessible at the Vercel URL
- All data pages show real stats from Google Sheets
- /debug/* routes return 404
- No console errors on page load (check browser DevTools)
</verification>

<success_criteria>
- Site is live on Vercel with a public URL
- SPREADSHEET_ID environment variable is configured
- All pages display correct, live data
- Build completed without errors on Vercel
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-launch/06-02-SUMMARY.md`
</output>
