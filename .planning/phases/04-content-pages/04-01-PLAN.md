---
phase: 04-content-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - content/news/round-one-playoff-preview.md
  - content/news/kind-of-new-and-somewhat-improved.md
  - content/news/new-seattle-wiffleball-website.md
  - content/news/its-crunch-time.md
  - content/news/in-season-tournament.md
  - content/news/season-preview.md
  - src/lib/news.ts
  - src/app/api/news/route.ts
  - src/app/news/[slug]/page.tsx
  - src/app/page.tsx
  - src/data/news.json
autonomous: true

must_haves:
  truths:
    - "News list page shows all articles with title, date, excerpt, and thumbnail image"
    - "Clicking an article opens its full content rendered from markdown with proper headings, bold, lists"
    - "Home page news preview section still displays featured + recent articles correctly"
    - "News articles are stored as individual .md files in content/news/ with YAML frontmatter"
  artifacts:
    - path: "content/news/round-one-playoff-preview.md"
      provides: "Migrated article with frontmatter"
      contains: "title:"
    - path: "src/lib/news.ts"
      provides: "News data access functions using gray-matter + remark"
      exports: ["getNewsArticles", "getFeaturedArticle", "getNewsArticle", "getNewsArticleWithHtml"]
    - path: "src/app/api/news/route.ts"
      provides: "News API endpoint for client components"
      exports: ["GET"]
  key_links:
    - from: "src/lib/news.ts"
      to: "content/news/"
      via: "fs.readdirSync + gray-matter"
      pattern: "readdirSync.*content/news"
    - from: "src/app/news/[slug]/page.tsx"
      to: "src/lib/news.ts"
      via: "await getNewsArticleWithHtml (async remark)"
      pattern: "await.*getNewsArticleWithHtml"
    - from: "src/app/page.tsx"
      to: "/api/news"
      via: "fetch in useEffect (client component cannot use fs)"
      pattern: "fetch.*/api/news"
---

<objective>
Refactor the news system from a single JSON file to individual markdown files with frontmatter, replacing the fragile regex-based markdown converter with remark + remark-html. Create an API route for client-side news consumers (home page).

Purpose: Enable easy content editing via individual .md files, fix markdown rendering bugs from the regex converter, and decouple news data access for both server and client components.
Output: 6 migrated .md article files in content/news/, refactored news lib, API route, updated pages.
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-pages/04-RESEARCH.md

@src/lib/news.ts
@src/data/news.json
@src/app/news/page.tsx
@src/app/news/[slug]/page.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps, migrate articles to markdown, refactor news lib, create API route</name>
  <files>
    package.json
    content/news/round-one-playoff-preview.md
    content/news/kind-of-new-and-somewhat-improved.md
    content/news/new-seattle-wiffleball-website.md
    content/news/its-crunch-time.md
    content/news/in-season-tournament.md
    content/news/season-preview.md
    src/lib/news.ts
    src/app/api/news/route.ts
  </files>
  <action>
    1. Run `npm install gray-matter remark-html` to add new dependencies. remark (^15.0.1) is already installed.

    2. Create `content/news/` directory at project root (not inside src/).

    3. Migrate each of the 6 articles from `src/data/news.json` into individual `.md` files in `content/news/`. Each file:
       - Named by slug (e.g., `round-one-playoff-preview.md`)
       - YAML frontmatter with: title, date (ISO format), excerpt, image (optional), tag (optional — derive from content: "Playoffs", "Preview", "Announcement", etc.), featured (boolean, only set if true)
       - Body is the article content field from JSON (already markdown-formatted)
       - Example frontmatter:
         ```
         ---
         title: "Round One Playoff Preview"
         date: "2025-08-15"
         excerpt: "The playoffs are here! With that comes a matchup..."
         image: "/images/news/2025-wild-card-weekend.jpg"
         tag: "Playoffs"
         featured: true
         ---
         ```

    4. Refactor `src/lib/news.ts` completely:
       - Remove the `import newsData from '@/data/news.json'` import
       - Remove the `convertMarkdownToHtml` function entirely
       - Import: `fs` from 'fs', `path` from 'path', `matter` from 'gray-matter', `{ remark }` from 'remark', `html` from 'remark-html'
       - Set `const newsDirectory = path.join(process.cwd(), 'content/news')`
       - Keep the `NewsArticle` interface but update it:
         ```typescript
         export interface NewsArticle {
           slug: string
           title: string
           date: string
           excerpt: string
           image?: string
           tag?: string
           featured?: boolean
         }
         ```
       - `getNewsArticles()`: Read all .md files from newsDirectory with fs.readdirSync, parse frontmatter with gray-matter, return sorted by date descending. This function is synchronous (uses readFileSync).
       - `getFeaturedArticle()`: Call getNewsArticles(), return first featured or first article. Keep same logic.
       - `getNewsArticle(slug)`: Read single .md file by slug, return frontmatter data or null if file not found. Wrap in try/catch for missing files.
       - `getNewsArticleWithHtml(slug)`: Make this function ASYNC. Read .md file, parse frontmatter with gray-matter, process markdown body with `await remark().use(html).process(content)`, return article data + htmlContent string.

    5. Create `src/app/api/news/route.ts`:
       - Import `getNewsArticles` from `@/lib/news`
       - Export async GET handler that returns `NextResponse.json({ data: getNewsArticles() })`
       - This endpoint exists because the home page is "use client" and cannot use fs-based news.ts directly. Server components (news/page.tsx) call the lib functions directly.
       - Follow existing API route pattern: try/catch with console.error and 500 response on error
  </action>
  <verify>
    - `npm run build` completes without errors
    - `content/news/` directory contains 6 .md files
    - Each .md file has valid YAML frontmatter (title, date, excerpt at minimum)
    - `src/lib/news.ts` no longer imports from `@/data/news.json`
    - `src/lib/news.ts` no longer contains `convertMarkdownToHtml`
  </verify>
  <done>
    News lib reads from content/news/*.md files using gray-matter + remark. API route serves article list for client consumers. All 6 articles migrated to individual markdown files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update news pages and home page, verify integration, clean up</name>
  <files>
    src/app/news/[slug]/page.tsx
    src/app/page.tsx
    src/data/news.json
  </files>
  <action>
    1. Update `src/app/news/[slug]/page.tsx`:
       - Make the component function `async` (Next.js 14 supports async server components)
       - Change `getNewsArticleWithHtml(params.slug)` to `await getNewsArticleWithHtml(params.slug)` since remark processing is now async
       - Add `<PageNavigation />` at the top of the JSX (import from `@/components/PageNavigation`) — currently missing, all other pages have it
       - Fix header background: change `bg-blue-600` to `bg-[#25397B]` for brand consistency
       - Fix link colors: change `text-blue-600` to `text-[#25397B]` and `hover:text-blue-800` to `hover:text-[#1e2f63]` (matching the news list page pattern)
       - Fix the featured badge colors: change `bg-blue-100 text-blue-800` to `bg-[#25397B] bg-opacity-20 text-[#25397B]` (matching news list page)

    2. Update `src/app/page.tsx` (home page):
       - Remove the import: `import { getNewsArticles } from '@/lib/news'` — this breaks because the home page is "use client" and getNewsArticles now uses Node.js `fs` module
       - Add the NewsArticle type import: copy the interface locally or import it (if exported as a type, use `import type { NewsArticle } from '@/lib/news'` — but this may not work if the module uses fs at the top level. Safer: define the type inline in page.tsx)
       - Replace the synchronous call `const articles = getNewsArticles()` with a useState + useEffect pattern:
         ```typescript
         const [articles, setArticles] = useState<NewsArticle[]>([])

         useEffect(() => {
           fetch('/api/news')
             .then(res => res.json())
             .then(json => setArticles(json.data || []))
             .catch(err => console.error('Error fetching news:', err))
         }, [])
         ```
       - The rest of the news rendering logic stays the same (featuredArticle = articles[0], recentArticles = articles.slice(1, 4))

    3. Delete `src/data/news.json` — all content now lives in content/news/*.md

    4. Verify the full chain:
       - `src/app/news/page.tsx` (server component) → calls getNewsArticles() directly → reads from filesystem ✓
       - `src/app/news/[slug]/page.tsx` (server component) → await getNewsArticleWithHtml() → remark processing ✓
       - `src/app/page.tsx` (client component) → fetch('/api/news') → API route → getNewsArticles() ✓
       - `npm run build` passes with no errors
  </action>
  <verify>
    - `npm run build` completes successfully
    - No references to `@/data/news.json` remain in codebase
    - `src/app/news/[slug]/page.tsx` contains `bg-[#25397B]` (not bg-blue-600)
    - `src/app/news/[slug]/page.tsx` contains `<PageNavigation`
    - `src/app/page.tsx` contains `fetch('/api/news')` (not direct import of getNewsArticles)
    - News list page (`/news`) renders articles
    - Article detail page (`/news/round-one-playoff-preview`) renders full markdown content
    - Home page news preview section renders featured + recent articles
  </verify>
  <done>
    All news pages render correctly using markdown-based article system. Home page fetches news via API. Article detail pages use brand colors and include PageNavigation. news.json is deleted.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` — no errors
2. Navigate to `/news` — all 6 articles displayed with title, date, excerpt, thumbnail
3. Click any article — full content renders with proper headings, bold text, lists, horizontal rules
4. Navigate to `/` (home page) — news preview shows featured article + 3 recent articles
5. Verify no import of `@/data/news.json` exists anywhere in src/
6. Verify `content/news/` has 6 .md files with valid frontmatter
</verification>

<success_criteria>
- News system reads from individual .md files, not from JSON
- remark + remark-html renders markdown correctly (headings, bold, lists, etc.)
- Home page news preview works via /api/news fetch
- Article detail pages have consistent brand colors (bg-[#25397B]) and PageNavigation
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-pages/04-01-SUMMARY.md`
</output>
