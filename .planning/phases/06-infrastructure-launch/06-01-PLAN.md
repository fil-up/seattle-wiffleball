---
phase: 06-infrastructure-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/debug/player-data/page.tsx
  - src/app/debug/team-mapping/page.tsx
  - src/components/StandingsDebug.tsx
  - src/components/TeamMapping.tsx
  - src/components/SheetrockStandings.tsx
  - src/components/StandingsTable.tsx
  - src/app/stats/teams/page.tsx
autonomous: true

must_haves:
  truths:
    - "No debug routes exist at /debug/* paths"
    - "No debug-only components exist in src/components/"
    - "SheetrockStandings is renamed to StandingsTable with no broken imports"
    - "Production build succeeds with zero errors"
  artifacts:
    - path: "src/components/StandingsTable.tsx"
      provides: "Renamed standings component (was SheetrockStandings)"
      contains: "StandingsTable"
  key_links:
    - from: "src/app/stats/teams/page.tsx"
      to: "src/components/StandingsTable.tsx"
      via: "import StandingsTable"
      pattern: "import StandingsTable from '@/components/StandingsTable'"
---

<objective>
Remove debug routes, delete debug-only components, and rename the legacy SheetrockStandings component. This satisfies INFRA-02 (unused deps already removed) and INFRA-03 (remove debug pages).

Purpose: Clean the codebase of development artifacts before deployment.
Output: A production-clean codebase that builds without errors.
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infrastructure-launch/06-RESEARCH.md
@src/app/stats/teams/page.tsx
@src/components/SheetrockStandings.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete debug routes and debug-only components</name>
  <files>
    src/app/debug/player-data/page.tsx
    src/app/debug/team-mapping/page.tsx
    src/components/StandingsDebug.tsx
    src/components/TeamMapping.tsx
  </files>
  <action>
    Delete the entire src/app/debug/ directory (contains player-data/page.tsx and team-mapping/page.tsx).
    Delete src/components/StandingsDebug.tsx.
    Delete src/components/TeamMapping.tsx.

    IMPORTANT: Do NOT delete PlayerBattingData.tsx or PlayerPitchingData.tsx — despite their debug-sounding names, they are used in production at src/app/stats/teams/[team]/page.tsx.

    After deletion, grep the entire src/ directory for any remaining imports of StandingsDebug or TeamMapping to confirm no dangling references exist.
  </action>
  <verify>
    Run: grep -r "StandingsDebug\|TeamMapping" src/ — should return zero results.
    Run: ls src/app/debug/ — should fail (directory gone).
  </verify>
  <done>Debug directory and both debug-only components are deleted with no remaining imports.</done>
</task>

<task type="auto">
  <name>Task 2: Rename SheetrockStandings to StandingsTable and verify build</name>
  <files>
    src/components/SheetrockStandings.tsx
    src/components/StandingsTable.tsx
    src/app/stats/teams/page.tsx
  </files>
  <action>
    1. Rename src/components/SheetrockStandings.tsx to src/components/StandingsTable.tsx.
    2. Inside StandingsTable.tsx, rename the component declaration from `SheetrockStandings` to `StandingsTable` (the const declaration and the default export).
    3. In src/app/stats/teams/page.tsx, update the import from `import SheetrockStandings from '@/components/SheetrockStandings'` to `import StandingsTable from '@/components/StandingsTable'`.
    4. Update the JSX usage from `<SheetrockStandings />` to `<StandingsTable />`.
    5. Run `npm run build` to verify the production build succeeds with zero TypeScript or ESLint errors.
  </action>
  <verify>
    Run: grep -r "SheetrockStandings\|Sheetrock" src/ — should return zero results.
    Run: npm run build — must succeed with no errors.
  </verify>
  <done>SheetrockStandings fully renamed to StandingsTable, all imports updated, production build passes.</done>
</task>

</tasks>

<verification>
- `ls src/app/debug/` fails (directory deleted)
- `grep -r "StandingsDebug\|TeamMapping\|SheetrockStandings" src/` returns zero results
- `npm run build` succeeds
- `src/components/StandingsTable.tsx` exists
- `src/components/PlayerBattingData.tsx` and `src/components/PlayerPitchingData.tsx` still exist (not deleted)
</verification>

<success_criteria>
- All debug routes and components removed
- SheetrockStandings renamed to StandingsTable
- Production build passes with zero errors
- No production components accidentally deleted
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-launch/06-01-SUMMARY.md`
</output>
