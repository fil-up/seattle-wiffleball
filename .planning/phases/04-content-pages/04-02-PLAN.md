---
phase: 04-content-pages
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - content/rules.md
  - src/app/rules/page.tsx
  - src/app/info/page.tsx
  - src/data/archives.json
  - src/components/SeasonAccordion.tsx
  - src/app/archives/page.tsx
autonomous: true

must_haves:
  truths:
    - "Rules page displays all 16 sections of league rules with numbered headings and formatted content"
    - "Info page shows league description, embedded Google Maps of Cowen Park, and a link to join"
    - "Archives page shows expandable season sections with champion, awards, and final standings"
    - "Clicking a season in Archives shows standings with W-L records for that year"
  artifacts:
    - path: "content/rules.md"
      provides: "Full rules content — 16 sections, ~100 rules"
      contains: "League Governance"
    - path: "src/app/rules/page.tsx"
      provides: "Rules page — async server component parsing markdown"
      contains: "PageNavigation"
    - path: "src/app/info/page.tsx"
      provides: "Info page — server component with map embed"
      contains: "google.com/maps"
    - path: "src/app/archives/page.tsx"
      provides: "Archives page — client component with accordion + standings fetch"
      contains: "use client"
    - path: "src/components/SeasonAccordion.tsx"
      provides: "Expandable season section component"
      exports: ["SeasonAccordion"]
    - path: "src/data/archives.json"
      provides: "Season champions and awards data"
      contains: "champion"
  key_links:
    - from: "src/app/rules/page.tsx"
      to: "content/rules.md"
      via: "fs.readFileSync + remark + remark-html"
      pattern: "readFileSync.*rules\\.md"
    - from: "src/app/archives/page.tsx"
      to: "/api/standings?scope=yearly"
      via: "fetch in useEffect"
      pattern: "fetch.*api/standings.*yearly"
    - from: "src/app/archives/page.tsx"
      to: "src/data/archives.json"
      via: "static import"
      pattern: "import.*archives\\.json"
    - from: "src/app/archives/page.tsx"
      to: "src/components/SeasonAccordion.tsx"
      via: "component import"
      pattern: "import.*SeasonAccordion"
---

<objective>
Build the Rules, Info, and Archives pages — three content-focused pages that complete the site's informational sections.

Purpose: Eliminate 404s for rules/info/archives routes and give users access to league rules, location info, and season history.
Output: Three new pages with full content, plus supporting data file and accordion component.
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-pages/04-RESEARCH.md
@.planning/phases/04-content-pages/04-01-SUMMARY.md

@src/components/PageNavigation.tsx
@src/app/api/standings/route.ts
@src/app/news/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Rules page and Info page</name>
  <files>
    content/rules.md
    src/app/rules/page.tsx
    src/app/info/page.tsx
  </files>
  <action>
    **Rules Page:**

    1. Create `content/rules.md` at project root with the full rules content from Seattle Wiffleball. The content has 16 numbered sections (0.00 through 15.00):
       - 0.00 League Governance
       - 1.00 Eligibility
       - 2.00 Scheduling
       - 3.00 Playoffs
       - 4.00 Game Management
       - 5.00 Umpiring
       - 6.00 Field Setup
       - 7.00 Equipment
       - 8.00 Pitching
       - 9.00 Batting
       - 10.00 At-Bats
       - 11.00 Base Running
       - 12.00 Fielding
       - 13.00 Tags and Plugs
       - 14.00 Dead Balls
       - 15.00 Respiratory Epidemic Exceptions

       Fetch and migrate the full content from https://seattlewiffleball.com/rules.html. Format as proper markdown with `##` for section headings and numbered sub-rules. Total: ~4,000 words, ~100 individual rules.

    2. Create `src/app/rules/page.tsx` as an async server component (NO "use client"):
       - Import: `fs` from 'fs', `path` from 'path', `{ remark }` from 'remark', `html` from 'remark-html', `PageNavigation` from '@/components/PageNavigation'
       - Read `content/rules.md` with `fs.readFileSync(path.join(process.cwd(), 'content/rules.md'), 'utf8')`
       - Process markdown: `const processed = await remark().use(html).process(rulesContent)`
       - Follow existing page pattern:
         ```tsx
         <div className="min-h-screen bg-gray-50">
           <PageNavigation />
           <div className="bg-[#25397B] text-white py-16">
             <div className="container mx-auto px-4">
               <h1 className="text-4xl md:text-6xl font-bold mb-4">League Rules</h1>
               <p className="text-xl md:text-2xl text-blue-100">Official Seattle Wiffleball League Rules & Regulations</p>
             </div>
           </div>
           <div className="container mx-auto px-4 py-16">
             <div className="max-w-4xl mx-auto bg-white rounded-lg shadow-md p-8 md:p-12">
               <div className="prose prose-lg max-w-none" dangerouslySetInnerHTML={{ __html: htmlContent }} />
             </div>
           </div>
         </div>
         ```

    **Info Page:**

    3. Create `src/app/info/page.tsx` as a server component (NO "use client"):
       - Import: `PageNavigation` from '@/components/PageNavigation'
       - Static content, no data fetching needed
       - Follow existing page pattern (PageNavigation + bg-[#25397B] header + container content)
       - Content sections:
         a. **About the League**: 2-3 paragraphs about Seattle Wiffleball League — competitive wiffleball in Seattle since 2015, plays at Cowen Park, summer season, emphasis on competition and community
         b. **Field Location**: Google Maps iframe embed for Cowen Park, Seattle. Use:
            ```tsx
            <iframe
              src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2687.5!2d-122.3159!3d47.6679!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x5490148e6be7ba01%3A0x28f1cfca6da89c16!2sCowen%20Park!5e0!3m2!1sen!2sus"
              width="100%"
              height="450"
              style={{ border: 0 }}
              allowFullScreen
              loading="lazy"
              referrerPolicy="no-referrer-when-downgrade"
              title="Cowen Park - Seattle Wiffleball field location"
            />
            ```
         c. **How to Join**: Short paragraph with an external link button. Use a placeholder URL (e.g., `https://forms.gle/placeholder`) with a comment indicating the user should replace it with the real signup URL.
         d. **Season Info**: Brief note about the typical season running May through August with playoffs in August/September
       - Style: use cards (bg-white rounded-lg shadow-md p-6) for each section, consistent spacing
  </action>
  <verify>
    - `npm run build` completes without errors
    - `content/rules.md` exists and contains all 16 section headings
    - `/rules` page renders formatted rules content with proper headings
    - `/info` page renders with map iframe, about section, and join link
    - Both pages include `<PageNavigation />` and use `bg-[#25397B]` header
    - Neither page contains `"use client"`
  </verify>
  <done>
    Rules page displays all 16 sections of league rules rendered from markdown. Info page shows league description, Cowen Park map, and how-to-join link.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Archives page with accordion and standings data</name>
  <files>
    src/data/archives.json
    src/components/SeasonAccordion.tsx
    src/app/archives/page.tsx
  </files>
  <action>
    1. Create `src/data/archives.json` with placeholder/template data for known seasons. Structure:
       ```json
       [
         {
           "year": 2025,
           "champion": "TBD",
           "runnerUp": "TBD",
           "mvp": null,
           "cyYoung": null,
           "battingChamp": null,
           "homeRunLeader": null,
           "notes": "Placeholder — update with actual 2025 results"
         },
         {
           "year": 2024,
           "champion": "TBD",
           "runnerUp": "TBD",
           "mvp": null,
           "cyYoung": null,
           "battingChamp": null,
           "homeRunLeader": null,
           "notes": "Placeholder — update with actual 2024 results"
         }
       ]
       ```
       Include entries for years 2015 through 2025 (11 seasons). All values are placeholders for the user to fill in. Use `null` for unknown award fields — the UI should handle null gracefully (don't render the row).

    2. Create `src/components/SeasonAccordion.tsx` as a client component:
       - `"use client"` directive at top
       - Props: `year: number`, `champion: string`, `children: React.ReactNode`, `defaultOpen?: boolean`
       - Uses `useState` for open/close toggle (default closed unless `defaultOpen` is true)
       - Renders a bordered card with:
         - Click-to-toggle header showing year + champion name + chevron icon
         - Expandable body with animated chevron rotation
         - Hover state on header (bg-gray-50)
       - Follow research example in 04-RESEARCH.md for the component structure

    3. Create `src/app/archives/page.tsx` as a client component (`"use client"`):
       - Import: `useState, useEffect` from 'react', `PageNavigation`, `SeasonAccordion`, `Link` from 'next/link', archives data from `@/data/archives.json`
       - Follow existing page pattern (PageNavigation + bg-[#25397B] header + container content)
       - On mount, fetch standings from `/api/standings?scope=yearly`
       - Process standings data:
         a. Group standings rows by year (standings have `{ team, year, wins, losses, pct, rf, ra }`)
         b. Sort years descending (most recent first)
         c. For each year, merge with archives.json to get champion/awards info
       - Render one `<SeasonAccordion>` per year:
         - Header shows year + champion name (from archives.json, or "Champion TBD" if not set)
         - Most recent year default open
         - Body contains:
           a. Awards section: MVP, Cy Young, Batting Champion, HR Leader (only show non-null values)
           b. Standings table: columns Team, W, L, PCT — sorted by PCT descending
           c. Link to stats page: `<Link href={'/stats/players?year=' + year}>View {year} Stats →</Link>` (cross-linking per CONTEXT.md)
       - Handle loading state: show spinner/message while standings fetch is in progress
       - Handle stale data: show stale banner if response includes `stale: true`
       - Handle empty state: "No standings data available" message if API returns empty
  </action>
  <verify>
    - `npm run build` completes without errors
    - `src/data/archives.json` contains entries for years 2015-2025
    - `/archives` page renders with accordion sections for each year that has standings data
    - Clicking a season expands to show standings table + awards
    - Most recent year accordion is expanded by default
    - Archives page includes PageNavigation and bg-[#25397B] header
    - "View Stats" links point to `/stats/players?year=YYYY`
    - Loading state shows while data is being fetched
  </verify>
  <done>
    Archives page displays expandable season sections with standings from API and champions/awards from JSON data. Each season links to the stats page for that year.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` — no errors
2. Navigate to `/rules` — all 16 rule sections display with proper formatting
3. Navigate to `/info` — about section, Google Maps embed, join link, season info visible
4. Navigate to `/archives` — accordion sections appear for each year, expanding shows standings + awards
5. Click "View Stats" link in archives — navigates to stats page (may not read year param yet, but link is correct)
6. All three pages have PageNavigation and consistent brand styling
</verification>

<success_criteria>
- Rules page renders full league rules from markdown with proper section formatting
- Info page shows league info, Cowen Park map iframe, and join link
- Archives page fetches standings, merges with JSON awards data, renders in accordion layout
- All pages follow the established pattern: PageNavigation + bg-[#25397B] header + container content
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-pages/04-02-SUMMARY.md`
</output>
