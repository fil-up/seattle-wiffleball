---
phase: 04-content-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/hall-of-fame.json
  - src/app/hall-of-fame/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /hall-of-fame shows a grid of inducted players with portrait images"
    - "Each player entry displays career stats, awards won, teams played for, and a written bio"
    - "Missing player images degrade gracefully with a placeholder"
  artifacts:
    - path: "src/data/hall-of-fame.json"
      provides: "Hall of Fame player data with stats, bios, and awards"
      contains: "inductionYear"
    - path: "src/app/hall-of-fame/page.tsx"
      provides: "Hall of Fame page with player grid and detail sections"
      min_lines: 60
  key_links:
    - from: "src/app/hall-of-fame/page.tsx"
      to: "src/data/hall-of-fame.json"
      via: "static JSON import"
      pattern: "import.*hall-of-fame"
---

<objective>
Build the Hall of Fame page displaying AI-generated player portraits, career statistics, awards, and biographical text for inducted players.

Purpose: The Hall of Fame (PAGE-10) celebrates the league's most accomplished players with a dedicated showcase page. This is a curated, read-only display that the user will populate with real data and AI-generated images over time.
Output: hall-of-fame.json data file with template entries for known inductees, and a Hall of Fame page with responsive card grid layout.
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-pages/04-RESEARCH.md
@.planning/phases/04-content-pages/04-CONTEXT.md

@src/components/PageNavigation.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hall of Fame data file with template entries</name>
  <files>src/data/hall-of-fame.json</files>
  <action>
    1. Create `src/data/hall-of-fame.json` with template entries for known Hall of Fame inductees.

    From news article content, two inductees are confirmed:
    - **Aaron Hunter** — described as "former MVP, Cy Young winner, and 2024 Hall of Fame inductee". Played for 100% Real Juice (the Juice). Known as elite pitcher with command.
    - **Epo Olivares** — described as "Hall of Famer and the league's all time strikeout king". Also played for 100% Real Juice (the Juice). Known as bullpen specialist.

    Data structure:
    ```json
    {
      "inductees": [
        {
          "id": "aaron-hunter",
          "name": "Aaron Hunter",
          "inductionYear": 2024,
          "image": "/images/hall-of-fame/aaron-hunter.png",
          "bio": "A cornerstone of the Juice franchise, Aaron Hunter established himself as one of the most dominant two-way players in league history. Known for his elite command on the mound and consistent production at the plate, Hunter's legacy spans multiple championship runs and individual accolades. His rising slider and veteran savvy made him a nightmare for opposing lineups throughout his career.",
          "seasonsPlayed": 8,
          "teams": ["100% Real Juice"],
          "awards": ["MVP", "Cy Young"],
          "careerStats": {
            "hitting": {
              "games": 0,
              "avg": 0,
              "homeRuns": 0,
              "rbis": 0,
              "ops": 0,
              "wrcPlus": 0
            },
            "pitching": {
              "wins": 0,
              "losses": 0,
              "era": 0,
              "strikeouts": 0,
              "whip": 0,
              "inningsPitched": 0
            }
          }
        },
        {
          "id": "epo-olivares",
          "name": "Epo Olivares",
          "inductionYear": 2024,
          "image": "/images/hall-of-fame/epo-olivares.png",
          "bio": "The league's all-time strikeout king, Epo Olivares carved out a legendary career as one of the most feared pitchers in Seattle Wiffleball history. A dominant reliever whose mononym status — up there with Bono, Ichiro, and Sheryl — speaks to his larger-than-life presence, Epo combined devastating stuff with an unflappable demeanor. When he entered the game, at-bats were over before they started.",
          "seasonsPlayed": 8,
          "teams": ["100% Real Juice"],
          "awards": ["All-Time Strikeout Leader"],
          "careerStats": {
            "hitting": {
              "games": 0,
              "avg": 0,
              "homeRuns": 0,
              "rbis": 0,
              "ops": 0,
              "wrcPlus": 0
            },
            "pitching": {
              "wins": 0,
              "losses": 0,
              "era": 0,
              "strikeouts": 0,
              "whip": 0,
              "inningsPitched": 0
            }
          }
        }
      ]
    }
    ```

    Notes on template data:
    - Career stats are set to 0 as placeholders — the user will fill in actual numbers from the stats infrastructure
    - `seasonsPlayed` is estimated at 8 (league started 2015, both played from early years)
    - Bios are written based on descriptions in the news articles — user can refine
    - Images point to `/images/hall-of-fame/` — user will provide AI-generated portraits
    - Add 1-2 more empty template entries commented out or as examples so the user sees the pattern for adding new inductees

    2. Create `public/images/hall-of-fame/` directory (empty — user provides images later). Create a `.gitkeep` file in it so the directory is tracked by git.
  </action>
  <verify>
    - `src/data/hall-of-fame.json` exists and is valid JSON
    - Contains at least 2 inductee entries with all required fields
    - `public/images/hall-of-fame/` directory exists
  </verify>
  <done>hall-of-fame.json contains 2 template entries for known inductees (Aaron Hunter, Epo Olivares) with full data structure. Image directory exists for future AI-generated portraits.</done>
</task>

<task type="auto">
  <name>Task 2: Create Hall of Fame page with player grid and detail sections</name>
  <files>src/app/hall-of-fame/page.tsx</files>
  <action>
    1. Create `src/app/hall-of-fame/page.tsx` as a **server component** (no "use client" — all data is static JSON, no interactivity needed).

    2. Follow established page pattern (PageNavigation + bg-[#25397B] header + container content).

    Title: "Hall of Fame" / subtitle: "Celebrating the legends of Seattle Wiffleball"

    3. Import inductees from `@/data/hall-of-fame.json`. Import `Image` from `next/image`.

    4. Layout: Display each inductee as a full-width card (stacked vertically, not a grid) so there's room for the portrait, stats, and bio. Each card should be visually prominent.

    Card structure per inductee:
    ```
    ┌──────────────────────────────────────────────┐
    │  ┌─────────┐  Name                           │
    │  │ Portrait │  Inducted: 2024                 │
    │  │  Image   │  Teams: Juice                   │
    │  │ (200x250)│  Seasons: 8                     │
    │  └─────────┘                                  │
    │                                               │
    │  Awards: MVP, Cy Young, ...                   │
    │                                               │
    │  Bio paragraph text...                        │
    │                                               │
    │  Career Stats                                 │
    │  ┌─────────────────┬─────────────────┐        │
    │  │ Hitting          │ Pitching        │        │
    │  │ AVG: .350        │ ERA: 2.10       │        │
    │  │ HR: 45           │ K: 312          │        │
    │  │ OPS: .950        │ WHIP: 0.89      │        │
    │  └─────────────────┴─────────────────┘        │
    └──────────────────────────────────────────────┘
    ```

    5. Specific implementation details:

    **Portrait image** — Use `next/image` with a fallback for missing images:
    ```tsx
    <div className="relative w-48 h-60 rounded-lg overflow-hidden bg-gray-200 flex-shrink-0">
      <Image
        src={inductee.image}
        alt={inductee.name}
        fill
        className="object-cover"
        onError={(e) => {
          // Can't use onError in server components — use a different approach
        }}
      />
    </div>
    ```

    IMPORTANT: Since this is a server component, you can't use `onError` for image fallback. Instead, check if the image file exists using `fs.existsSync(path.join(process.cwd(), 'public', inductee.image))`. If it doesn't exist, render a placeholder div with the player's initials instead:
    ```tsx
    <div className="w-48 h-60 rounded-lg bg-gradient-to-b from-[#25397B] to-[#1e2f63] flex items-center justify-center flex-shrink-0">
      <span className="text-5xl font-bold text-white/50">
        {inductee.name.split(' ').map(n => n[0]).join('')}
      </span>
    </div>
    ```

    **Awards badges** — Display each award as a styled badge:
    ```tsx
    <div className="flex flex-wrap gap-2">
      {inductee.awards.map(award => (
        <span key={award} className="bg-yellow-100 text-yellow-800 text-sm font-medium px-3 py-1 rounded-full">
          🏆 {award}
        </span>
      ))}
    </div>
    ```

    **Career stats** — Display in a two-column grid (Hitting | Pitching). Only show sections where stats are non-zero. Use a clean table or stat grid layout:
    ```tsx
    <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
      {/* Hitting stats - only if any hitting stats > 0 */}
      {(inductee.careerStats.hitting.games > 0) && (
        <div>
          <h4 className="font-bold text-gray-900 mb-3">Hitting</h4>
          <div className="grid grid-cols-3 gap-3">
            <StatBox label="AVG" value={inductee.careerStats.hitting.avg} />
            <StatBox label="HR" value={inductee.careerStats.hitting.homeRuns} />
            <StatBox label="OPS" value={inductee.careerStats.hitting.ops} />
            {/* etc. */}
          </div>
        </div>
      )}
      {/* Pitching stats - similar */}
    </div>
    ```

    Create a small `StatBox` component (can be inline in the file) that displays a label and value in a compact format.

    **Bio** — Render as paragraphs with `text-gray-700 leading-relaxed`.

    **Teams** — Show as comma-separated list or badges.

    6. Handle the empty state: if `inductees` array is empty, show a message like "Hall of Fame inductees coming soon."

    7. Handle all-zero stats gracefully: if all career stats are 0 (placeholder data), show "Stats pending" instead of a table full of zeros.

    8. Sort inductees by `inductionYear` descending (most recent first), then by name.

    9. Add a brief intro paragraph below the header explaining the Hall of Fame: "The Seattle Wiffleball Hall of Fame honors players who have made an extraordinary impact on the league through their skill, sportsmanship, and dedication to the game."
  </action>
  <verify>
    - `npm run build` completes without errors
    - Visit /hall-of-fame — should show inductee cards for Aaron Hunter and Epo Olivares
    - Each card shows: name, induction year, teams, awards, bio text
    - Missing images show placeholder with initials (since AI images aren't provided yet)
    - Career stats show "Stats pending" if all zeros
    - Page follows established design pattern (PageNavigation + brand header)
  </verify>
  <done>Hall of Fame page at /hall-of-fame displays inducted players with portrait placeholders, bios, awards badges, and career stats sections. Gracefully handles missing images and placeholder data.</done>
</task>

</tasks>

<verification>
1. Visit /hall-of-fame — inductee cards displayed with correct data
2. Portrait placeholders render (initials on brand-color gradient) since images don't exist yet
3. Awards display as styled badges
4. Bio text is readable and well-formatted
5. `npm run build` — no build errors
</verification>

<success_criteria>
- Hall of Fame page renders all inductees from hall-of-fame.json
- Player cards show portrait (or placeholder), name, induction year, teams, awards, bio, and career stats
- Missing images handled gracefully without broken image icons
- Zero stats show "Stats pending" instead of zeros
- Data file is structured for easy user updates (add more inductees, fill in stats)
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-pages/04-03-SUMMARY.md`
</output>
