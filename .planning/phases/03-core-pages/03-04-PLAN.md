---
phase: 03-core-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sheets.ts
  - src/components/LeaderboardPodium.tsx
  - src/app/leaderboards/page.tsx
  - src/app/api/leaderboards/route.ts
autonomous: true

must_haves:
  truths:
    - "Leaderboards page shows podium-style display with top 3 players visually prominent"
    - "Ranks 4-10 are shown in a compact list below the podium"
    - "Hitting categories include AVG, HR, RBI, wRC+"
    - "Pitching categories include ERA, K, W, WHIP, IP, K/9, OPP AVG"
    - "K/9 stat is correctly calculated from strikeouts and innings pitched"
    - "Year selector defaults to current season and allows switching years"
    - "Clicking a player name navigates to their player detail page"
  artifacts:
    - path: "src/lib/sheets.ts"
      provides: "K/9 calculated field in pitching transforms"
      contains: "k9"
    - path: "src/components/LeaderboardPodium.tsx"
      provides: "Podium display component for top 10 rankings"
      exports: ["LeaderboardPodium"]
    - path: "src/app/leaderboards/page.tsx"
      provides: "Podium-style leaderboards page"
      min_lines: 100
    - path: "src/app/api/leaderboards/route.ts"
      provides: "Leaderboard API with oppAvg ascending sort"
      contains: "oppAvg"
  key_links:
    - from: "src/app/leaderboards/page.tsx"
      to: "src/components/LeaderboardPodium.tsx"
      via: "import and render for each stat category"
      pattern: "LeaderboardPodium"
    - from: "src/app/leaderboards/page.tsx"
      to: "/api/leaderboards"
      via: "fetch with stat param for each category"
      pattern: "fetch.*api/leaderboards"
    - from: "src/app/api/leaderboards/route.ts"
      to: "src/lib/sheets.ts"
      via: "uses pitching transforms that now include k9"
      pattern: "transformYearlyPitching|transformTotalsPitching"
---

<objective>
Add K/9 to pitching data, build the podium display component, and transform the leaderboards page from plain tables to a visually striking podium layout.

Purpose: Leaderboards are a signature page — the podium style makes top performers feel celebrated rather than just listed. Adding K/9 and OPP AVG completes the pitching stat coverage.
Output: 1 new component, 2 modified data files, 1 transformed page.
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages/03-RESEARCH.md

@src/lib/sheets.ts
@src/app/leaderboards/page.tsx
@src/app/api/leaderboards/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add K/9 to pitching transforms and fix leaderboard API sort</name>
  <files>src/lib/sheets.ts, src/app/api/leaderboards/route.ts</files>
  <action>
**sheets.ts — Add K/9 to pitching transforms:**

1. In `transformYearlyPitching`: after the existing `whip` field, add:
   ```
   k9: inningsPitched > 0 ? parseFloat(((strikeouts / inningsPitched) * 9).toFixed(2)) : 0,
   ```
   Where `inningsPitched` and `strikeouts` are the local variables already extracted in the same transform (from `cellFloat` and `cellNumber` calls). You need to extract them to local variables before the return object so they can be used in the calculation:
   ```
   const inningsPitched = cellFloat(row, colIdx(cm, 'ip', 8), 1)
   const strikeouts = cellNumber(row, colIdx(cm, 'k', 14))
   ```
   Then use these variables both in the return object fields AND in the k9 calculation.

2. In `transformTotalsPitching`: same pattern — add `k9` calculated field after `whip`. Extract `inningsPitched` and `strikeouts` to local variables.

3. Also verify that `oppAvg` exists in `transformYearlyPitching`. If it's missing (currently only in totals transform), add it:
   ```
   oppAvg: cellFloat(row, colIdx(cm, 'opp avg', 21), 3),
   ```
   Check the actual column index by looking at the yearly pitching data columns.

4. Update the TypeScript type interfaces `YearlyPitchingRow` and `TotalsPitchingRow` (if they exist as explicit interfaces) to include `k9: number`. If the types are inferred, no change needed.

**leaderboards/route.ts — Add oppAvg to ascending sort:**

1. The current `ASCENDING_STATS` set is `new Set(['era', 'whip'])`.
2. Add `'oppAvg'` to the set: `new Set(['era', 'whip', 'oppAvg'])`.
   Reason: Lower opponent batting average is better for pitchers, so oppAvg should sort ascending.
3. K/9 should sort DESCENDING (more strikeouts per 9 innings is better) — it's already the default, no change needed.
  </action>
  <verify>
- `npx tsc --noEmit` passes.
- `k9` field exists in both `transformYearlyPitching` and `transformTotalsPitching` return objects.
- `oppAvg` exists in both yearly and totals pitching transforms.
- ASCENDING_STATS in leaderboards/route.ts includes 'oppAvg'.
  </verify>
  <done>
- K/9 stat is calculated as (K / IP) * 9 with division-by-zero protection.
- oppAvg is available in both yearly and totals pitching data.
- Leaderboard API sorts oppAvg ascending (lower is better for pitchers).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create LeaderboardPodium component and transform leaderboards page</name>
  <files>src/components/LeaderboardPodium.tsx, src/app/leaderboards/page.tsx</files>
  <action>
**LeaderboardPodium.tsx:**

Create `src/components/LeaderboardPodium.tsx`:
1. No `"use client"` needed (it receives data as props, no hooks).
2. Props:
   ```
   {
     title: string           // e.g. "AVG", "ERA"
     players: Array<{
       rank: number
       name: string
       playerId: string
       value: string | number
       team?: string
     }>
     formatValue?: (v: number) => string  // optional formatter
   }
   ```
3. Podium layout (top 3):
   - Three cards arranged with flexbox: `flex items-end justify-center gap-3 md:gap-4 mb-4`.
   - Order in DOM: 2nd place (left), 1st place (center), 3rd place (right).
   - 1st place card: tallest, `border-t-4 border-yellow-500 bg-yellow-50`, gold "1" badge, larger text.
   - 2nd place card: medium height, `border-t-4 border-gray-400 bg-gray-50`, silver "2" badge.
   - 3rd place card: shortest, `border-t-4 border-amber-700 bg-orange-50`, bronze "3" badge.
   - Each card shows: rank badge (large number with accent color), player name (Link to `/stats/players/${playerId}`), team name (small text), stat value (large bold).
   - Height differentiation: use padding — 1st gets `pt-6 pb-4`, 2nd gets `pt-4 pb-3`, 3rd gets `pt-3 pb-3`.
   - Card width: `w-1/3` for each.
4. Compact list (ranks 4-10):
   - Simple rows below the podium.
   - Each row: `flex justify-between items-center py-2 border-b border-gray-100`.
   - Left: rank number + player name (Link).
   - Right: stat value.
5. If fewer than 3 players, adjust gracefully (show what's available, no podium).
6. Title shown above the podium as `text-lg font-semibold`.

**Leaderboards page transformation:**

Rewrite `src/app/leaderboards/page.tsx`:

1. **Update stat configurations:**
   - HITTING_STATS: `[{ key: "avg", label: "AVG" }, { key: "homeRuns", label: "HR" }, { key: "rbis", label: "RBI" }, { key: "wrcPlus", label: "wRC+" }]` — CONTEXT.md specifies AVG, HR, RBI, wRC+ (not OPS, OBP, SLG).
   - PITCHING_STATS: `[{ key: "era", label: "ERA" }, { key: "strikeouts", label: "K" }, { key: "wins", label: "W" }, { key: "whip", label: "WHIP" }, { key: "inningsPitched", label: "IP" }, { key: "k9", label: "K/9" }, { key: "oppAvg", label: "OPP AVG" }]`.

2. **Add stat formatting map:**
   ```
   const FORMATTERS: Record<string, (v: number) => string> = {
     avg: v => v.toFixed(3),
     obp: v => v.toFixed(3),
     slg: v => v.toFixed(3),
     ops: v => v.toFixed(3),
     oppAvg: v => v.toFixed(3),
     era: v => v.toFixed(2),
     whip: v => v.toFixed(2),
     k9: v => v.toFixed(2),
     inningsPitched: v => v.toFixed(1),
     wrcPlus: v => v.toFixed(0),
   }
   ```
   Default: show as-is (integers for HR, RBI, K, W).

3. **Replace SmallTable with LeaderboardPodium:**
   - Remove the inline `SmallTable` component entirely.
   - Import `LeaderboardPodium` from `@/components/LeaderboardPodium`.
   - For each stat, render `<LeaderboardPodium>` with the fetched data.
   - Map API response rows to the podium's expected format: `{ rank: idx+1, name: r.player?.name, playerId: r.playerId, value: formatted stat, team: r.team }`.

4. **Layout:**
   - Keep the year selector at top.
   - "Hitting" section: render podiums in a responsive grid `grid-cols-1 md:grid-cols-2 gap-6 mb-10`.
   - "Pitching" section: same grid layout. Since there are 7 pitching stats, the last one will take full width on md screens (which is fine).
   - Each podium is in a `bg-white rounded-lg shadow p-4 md:p-6` card.

5. **Data fetching:**
   - Keep the existing pattern — fetch each stat from `/api/leaderboards` with category, year, stat, limit=10 params.
   - Add the new pitching stats to the fetch list (W, IP, K/9, OPP AVG).
   - Keep minSeasons=3 for all-time.

6. **Player links:**
   - LeaderboardPodium handles linking via the playerId prop.
   - The leaderboard API already returns `playerId` in the data.

7. **Keep existing:**
   - Year selector with "All-Time" option.
   - PageNavigation at top.
   - Stale data banner.
   - Loading state.
  </action>
  <verify>
- `npx tsc --noEmit` passes.
- LeaderboardPodium.tsx exists and exports LeaderboardPodium component.
- Leaderboards page imports LeaderboardPodium (not SmallTable).
- PITCHING_STATS includes k9 and oppAvg.
- HITTING_STATS matches CONTEXT.md spec: AVG, HR, RBI, wRC+.
  </verify>
  <done>
- Leaderboards page shows podium-style rankings with top 3 visually prominent.
- All 4 hitting categories and 7 pitching categories are displayed.
- K/9 and OPP AVG stats show correct values from the data.
- Player names link to player detail pages.
- Year selector switches between seasons and all-time.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors.
2. K/9 field exists in pitching data (check by calling /api/stats?category=pitching&limit=1 and inspecting response).
3. Leaderboards page renders podiums instead of tables.
4. All 11 stat categories display (4 hitting + 7 pitching).
5. oppAvg sorts ascending in leaderboard results.
6. `npm run build` completes without errors.
</verification>

<success_criteria>
- Leaderboards page has a sports-awards feel with podium prominence for top 3.
- All stat categories from CONTEXT.md are present: AVG, HR, RBI, wRC+ (hitting), ERA, K, W, WHIP, IP, K/9, OPP AVG (pitching).
- K/9 is correctly calculated and sorts correctly (higher is better).
- OPP AVG sorts correctly (lower is better).
- Player names are clickable links to detail pages.
- Year navigation works for all categories.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages/03-04-SUMMARY.md`
</output>
