---
phase: 05-design-polish
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/app/news/page.tsx
  - src/app/news/[slug]/page.tsx
  - src/app/rules/page.tsx
  - src/app/archives/page.tsx
  - src/app/info/page.tsx
  - src/app/hall-of-fame/page.tsx
  - src/app/schedule/page.tsx
  - src/app/media/page.tsx
  - src/components/SeasonAccordion.tsx
autonomous: true

must_haves:
  truths:
    - "No content page renders its own navigation bar"
    - "All content pages display correctly in both light and dark mode"
    - "News, archives, and media pages show skeleton loading while fetching data"
    - "Rules page prose content is readable in dark mode"
    - "Content pages with data fetching show error states on failure"
  artifacts:
    - path: "src/app/news/page.tsx"
      provides: "News listing with theme tokens and loading/error states"
    - path: "src/app/rules/page.tsx"
      provides: "Rules page with dark-mode-compatible prose"
    - path: "src/app/archives/page.tsx"
      provides: "Archives with theme tokens and loading skeleton"
  key_links:
    - from: "src/app/rules/page.tsx"
      to: "dark mode prose"
      via: "dark:prose-invert class"
      pattern: "prose.*dark:prose-invert"
    - from: "content page files"
      to: "src/components/Skeleton.tsx"
      via: "import and render while loading"
      pattern: "import.*Skeleton"
---

<objective>
Migrate all content pages (news, rules, archives, info, hall of fame, schedule, media) to the new theme system: remove per-page navigation, apply theme tokens, and add appropriate loading/error states.

Purpose: Content pages complete the site-wide theme migration. After this plan, every page in the site uses the unified design system.
Output: All 8 content pages themed with dark mode support, loading skeletons, and error handling.
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-design-polish/05-01-SUMMARY.md
@src/app/news/page.tsx
@src/app/news/[slug]/page.tsx
@src/app/rules/page.tsx
@src/app/archives/page.tsx
@src/app/info/page.tsx
@src/app/hall-of-fame/page.tsx
@src/app/schedule/page.tsx
@src/app/media/page.tsx
@src/components/SeasonAccordion.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old navigation and apply theme tokens to content pages</name>
  <files>src/app/news/page.tsx, src/app/news/[slug]/page.tsx, src/app/rules/page.tsx, src/app/archives/page.tsx, src/app/info/page.tsx, src/app/hall-of-fame/page.tsx, src/app/schedule/page.tsx, src/app/media/page.tsx, src/components/SeasonAccordion.tsx</files>
  <action>
    **Step 1: Remove PageNavigation from all 8 content pages**
    For each page, remove the `import PageNavigation from '@/components/PageNavigation'` line and every `<PageNavigation />` JSX usage:
    - src/app/news/page.tsx
    - src/app/news/[slug]/page.tsx
    - src/app/rules/page.tsx
    - src/app/archives/page.tsx
    - src/app/info/page.tsx
    - src/app/hall-of-fame/page.tsx
    - src/app/schedule/page.tsx
    - src/app/media/page.tsx

    NOTE: PageNavigation.tsx itself was already deleted in Plan 05-02. If 05-03 runs in parallel with 05-02, the file may still exist — that's fine, just remove the imports from content pages regardless.

    **Step 2: Apply theme color tokens**
    Same mapping as 05-02 Task 1, applied to all 8 content pages and the SeasonAccordion component:

    | Old Class | New Class |
    |-----------|-----------|
    | `bg-gray-50` | `bg-surface-secondary` |
    | `bg-white` | `bg-surface-card` |
    | `bg-gray-100` | `bg-surface-secondary` |
    | `text-gray-900` | `text-content-primary` |
    | `text-gray-800` | `text-content-primary` |
    | `text-gray-700` | `text-content-primary` |
    | `text-gray-600` | `text-content-secondary` |
    | `text-gray-500` | `text-content-secondary` |
    | `text-gray-400` | `text-content-secondary` |
    | `border-gray-200` | `border-border` |
    | `border-gray-300` | `border-border` |
    | `divide-gray-200` | `divide-border` |
    | `bg-[#25397B]` | `bg-brand-navy` |
    | `text-blue-600` | `text-brand-navy` |
    | `hover:text-blue-800` | `hover:text-brand-navy/80` |
    | `hover:bg-gray-50` | `hover:bg-table-hover` |
    | `hover:bg-gray-100` | `hover:bg-table-hover` |
    | `shadow-md` | `shadow-md` (keep — shadows work in both modes) |

    **Step 3: Rules page dark mode prose**
    The rules page uses @tailwindcss/typography `prose` classes for markdown rendering. Add `dark:prose-invert` alongside the existing `prose` class to ensure text is readable in dark mode. Example: `className="prose dark:prose-invert max-w-none"`

    **Step 4: Hall of Fame dark mode compatibility**
    - The hall of fame page has custom card styling with portrait images and bios
    - Ensure card backgrounds use `bg-surface-card`, text uses `text-content-primary`, and borders use `border-border`
    - The initials circle fallback should use `bg-brand-navy text-white` (consistent in both modes)

    **Step 5: Info page map embed**
    - The info page has an embedded Google Map — this doesn't need theming (iframe content is external)
    - Apply theme tokens to surrounding text and layout only

    **Step 6: Restyle stale data banners in content pages**
    Same as 05-02: replace any `bg-yellow-50` stale banners with `bg-brand-gold/10 border border-brand-gold/30 text-content-primary`
  </action>
  <verify>
    - `npx next build` compiles without errors
    - No content page imports PageNavigation
    - Rules page has `dark:prose-invert` class
    - All content pages use theme tokens instead of hardcoded gray colors
  </verify>
  <done>All 8 content pages use the unified navigation from layout.tsx and theme tokens for colors. Prose content is readable in dark mode. Hall of Fame cards and Info page styled consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Add loading skeletons and error states to content data-fetching pages</name>
  <files>src/app/news/page.tsx, src/app/archives/page.tsx, src/app/media/page.tsx</files>
  <action>
    Only pages that fetch data client-side need skeleton/error states. Server-rendered pages (rules, info, hall-of-fame, news/[slug]) already handle errors at the server level.

    **Import in each applicable file:**
    ```tsx
    import { CardSkeleton, TableSkeleton } from '@/components/Skeleton'
    import ErrorState from '@/components/ErrorState'
    ```

    **Add error tracking and retry pattern** (same pattern as 05-02):
    ```tsx
    const [error, setError] = useState(false)
    const [retryCount, setRetryCount] = useState(0)
    // Add retryCount to useEffect deps
    // In catch: setError(true)
    // In ErrorState: onRetry={() => { setError(false); setRetryCount(c => c + 1) }}
    ```

    **Per-page replacements:**

    1. **News listing (src/app/news/page.tsx)**:
       - If it has a loading state, replace with grid of `<CardSkeleton />` (3-4 cards matching article card layout)
       - Add error state with retry

    2. **Archives (src/app/archives/page.tsx)**:
       - Replace loading state with `<TableSkeleton rows={8} cols={4} />` for standings data
       - Add error state with retry

    3. **Media (src/app/media/page.tsx)**:
       - Replace loading state with grid of `<CardSkeleton />` (3 cards matching video card layout)
       - Add error state with retry

    **Server-rendered pages (no client-side loading states needed):**
    - news/[slug]: Server component — if markdown file not found, show a simple "Article not found" message styled with theme tokens
    - rules: Server component — static content, no loading state needed
    - info: Static page — no loading state needed
    - hall-of-fame: Static data — no loading state needed
    - schedule: GameChanger widget handles its own loading internally
  </action>
  <verify>
    - `npx next build` compiles without errors
    - News page shows card skeletons while loading articles
    - Archives page shows table skeleton while loading standings data
    - Media page shows card skeletons while loading videos
    - Error states display with retry buttons on fetch failure
  </verify>
  <done>All content pages with client-side data fetching show skeleton loading placeholders and error states with retry. Server-rendered content pages handle errors gracefully.</done>
</task>

</tasks>

<verification>
1. `npx next build` passes
2. `grep -r "PageNavigation" src/` returns zero results across entire codebase
3. Visit each content page in dev mode in both light and dark mode
4. Rules page prose text is readable in dark mode
5. News, archives, media pages show skeletons during loading
</verification>

<success_criteria>
- Zero content pages import or render PageNavigation
- All 8 content pages use theme tokens
- Rules prose has dark:prose-invert for dark mode readability
- News, archives, media pages show skeletons while loading
- Error states display on fetch failures with retry
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-polish/05-03-SUMMARY.md`
</output>
