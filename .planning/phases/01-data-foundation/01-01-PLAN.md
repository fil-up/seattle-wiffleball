---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.local
  - .env.example
  - package.json
  - prisma/schema.prisma
  - src/lib/prisma.ts
  - scripts/import-data.ts
  - src/app/api/admin/import/route.ts
  - src/app/api/players/route.ts
  - src/app/api/players/[id]/route.ts
  - src/app/api/teams/[id]/route.ts
  - src/app/api/stats/route.ts
  - src/app/api/leaderboards/route.ts
autonomous: true

must_haves:
  truths:
    - "No Prisma schema, SQLite database file, or Prisma client code exists in the codebase"
    - "The spreadsheet ID is configured in exactly one environment variable (SPREADSHEET_ID in .env.local)"
    - "No imports of @prisma/client or @/lib/prisma remain in any source file"
    - "npm run build succeeds without Prisma-related errors"
    - "package.json contains no Prisma-related dependencies or scripts"
  artifacts:
    - path: ".env.local"
      provides: "SPREADSHEET_ID environment variable"
      contains: "SPREADSHEET_ID=1bkQNmqFBqBqyqwo5vtNZHSDYTwvU1_WSsfE2GlBd3Ek"
    - path: ".env.example"
      provides: "Template documenting required environment variables"
      contains: "SPREADSHEET_ID="
  key_links:
    - from: ".env.local"
      to: "src/lib/sheets.ts (created in Plan 01-02)"
      via: "process.env.SPREADSHEET_ID"
      pattern: "SPREADSHEET_ID"
---

<objective>
Remove all Prisma/SQLite artifacts from the codebase and centralize the Google Sheets spreadsheet ID in a single environment variable. After this plan, the codebase has zero Prisma dependencies, builds cleanly, and is ready for the shared Google Sheets utility built in Plan 01-02.

Purpose: Eliminate the dead Prisma/SQLite data path so Phase 1 can build a clean, single-source pipeline from Google Sheets.
Output: A buildable codebase with no Prisma remnants and a centralized SPREADSHEET_ID env var.
</objective>

<execution_context>
@C:\Users\phill\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\phill\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@package.json
@prisma/schema.prisma
@src/lib/prisma.ts
@scripts/import-data.ts
@src/app/api/admin/import/route.ts
@src/app/api/players/route.ts
@src/app/api/players/[id]/route.ts
@src/app/api/teams/[id]/route.ts
@src/app/api/stats/route.ts
@src/app/api/leaderboards/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create environment files for spreadsheet configuration</name>
  <files>.env.local, .env.example</files>
  <action>
    Create `.env.local` with the spreadsheet ID that is currently hardcoded across the codebase:
    ```
    SPREADSHEET_ID=1bkQNmqFBqBqyqwo5vtNZHSDYTwvU1_WSsfE2GlBd3Ek
    ```

    Create `.env.example` as a template for other developers/deployments:
    ```
    # Google Sheets spreadsheet ID — the single source of truth for all league data
    SPREADSHEET_ID=
    ```

    Confirm `.gitignore` already contains `.env.local` (it does — no changes needed to .gitignore).

    Use `SPREADSHEET_ID` (NOT `NEXT_PUBLIC_SPREADSHEET_ID`) because all fetches will go through server-side API routes. The spreadsheet ID must never be exposed to the browser.
  </action>
  <verify>Confirm .env.local exists and contains the SPREADSHEET_ID. Confirm .env.example exists with the placeholder.</verify>
  <done>.env.local contains SPREADSHEET_ID=1bkQNmqFBqBqyqwo5vtNZHSDYTwvU1_WSsfE2GlBd3Ek and .env.example documents the required variable.</done>
</task>

<task type="auto">
  <name>Task 2: Remove all Prisma/SQLite artifacts and unused dependencies</name>
  <files>prisma/schema.prisma, src/lib/prisma.ts, scripts/import-data.ts, src/app/api/admin/import/route.ts, src/app/api/players/route.ts, src/app/api/players/[id]/route.ts, src/app/api/teams/[id]/route.ts, src/app/api/stats/route.ts, src/app/api/leaderboards/route.ts, package.json</files>
  <action>
    **Step 1: Delete Prisma infrastructure files**
    Delete these files entirely:
    - `prisma/schema.prisma` (Prisma schema)
    - `src/lib/prisma.ts` (Prisma client singleton)
    - `scripts/import-data.ts` (Prisma import script)
    - `src/app/api/admin/import/route.ts` (admin import trigger endpoint)

    Delete the empty directories if they become empty after file deletion:
    - `prisma/` directory
    - `scripts/` directory
    - `src/app/api/admin/import/` directory
    - `src/app/api/admin/` directory (if empty)

    Also delete any `*.db` files in the project root or prisma/ directory (SQLite database files). There may be none — that's fine.

    **Step 2: Stub Prisma-dependent API routes**
    These 5 routes currently import from `@/lib/prisma` which was just deleted. Replace each file's entire contents with a minimal 503 stub so the build doesn't break. Plan 01-02 will rewrite these with real Google Sheets implementations.

    For routes WITHOUT URL params (`src/app/api/players/route.ts`, `src/app/api/stats/route.ts`, `src/app/api/leaderboards/route.ts`), replace with:
    ```typescript
    import { NextResponse } from 'next/server'

    export async function GET() {
      return NextResponse.json(
        { error: 'Route being migrated to Google Sheets data source' },
        { status: 503 }
      )
    }
    ```

    For routes WITH URL params (`src/app/api/players/[id]/route.ts`, `src/app/api/teams/[id]/route.ts`), replace with:
    ```typescript
    import { NextResponse } from 'next/server'

    export async function GET(
      request: Request,
      { params }: { params: { id: string } }
    ) {
      return NextResponse.json(
        { error: 'Route being migrated to Google Sheets data source' },
        { status: 503 }
      )
    }
    ```

    **Step 3: Uninstall unused packages**
    Run: `npm uninstall @prisma/client prisma googleapis axios sheetrock graceful-fs`

    - `@prisma/client` + `prisma`: Prisma ORM (no longer used)
    - `googleapis`: Only used by the deleted import script
    - `axios`: Only used by the deleted import script
    - `sheetrock`: Listed as dependency but never imported in source code
    - `graceful-fs`: Listed as dependency but not directly used

    **Step 4: Clean up package.json scripts**
    Remove these scripts from package.json:
    - `"prisma:generate": "prisma generate"`
    - `"prisma:push": "prisma db push"`
    - `"import": "ts-node --transpile-only scripts/import-data.ts"`

    Also remove `ts-node` from devDependencies since it was only used by the import script.
  </action>
  <verify>
    Run these checks:
    1. Confirm deleted files are gone: `prisma/schema.prisma`, `src/lib/prisma.ts`, `scripts/import-data.ts`, `src/app/api/admin/import/route.ts` should not exist
    2. Confirm no Prisma imports remain: search for `@prisma/client` and `@/lib/prisma` across all source files — should return zero results
    3. Confirm package.json has no Prisma-related dependencies or scripts
    4. Run `npm install` to ensure lock file is consistent
    5. Run `npm run build` to confirm the project builds without errors (stubbed routes should compile fine)
  </verify>
  <done>
    All Prisma/SQLite artifacts deleted. Five Prisma-dependent routes stubbed with 503 responses. All unused packages uninstalled. package.json clean of Prisma scripts. Project builds successfully.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `.env.local` exists with `SPREADSHEET_ID=1bkQNmqFBqBqyqwo5vtNZHSDYTwvU1_WSsfE2GlBd3Ek`
2. No file in `src/` imports from `@prisma/client` or `@/lib/prisma`
3. No files exist in `prisma/`, `scripts/`, or `src/app/api/admin/`
4. `package.json` has zero Prisma-related entries (deps, devDeps, scripts)
5. `npm run build` succeeds
</verification>

<success_criteria>
- The codebase contains zero Prisma/SQLite artifacts
- SPREADSHEET_ID is the single environment variable for sheet configuration
- The project builds cleanly with stubbed API routes
- No unused dependencies remain in package.json
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
